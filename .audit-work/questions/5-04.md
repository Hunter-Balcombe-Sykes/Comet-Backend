---
item_id: #5-04
session_id: 68c2dfc8-fc93-4366-8ac0-239b8ba1970b
written_at: 2026-05-05T00:37:08+00:00
---

# False Positive — Constraint Already Exists

The unique constraint `(professional_id, fresha_variation_id)` that this item asks to add **already exists** in the baseline migration.

**Evidence:**

File: `supabase/migrations/20260403000000_v2_baseline.sql`, lines 678–679:

```sql
ALTER TABLE site.services
    ADD CONSTRAINT services_professional_fresha_variation_uq UNIQUE (professional_id, fresha_variation_id);
```

No subsequent migration drops or modifies this constraint. Grepping all `.sql` files in `supabase/migrations/` for `services_professional_fresha_variation_uq` returns only this one ADD CONSTRAINT line.

**Why I stopped:**

Adding a duplicate migration would cause `ALTER TABLE ... ADD CONSTRAINT` to fail at deployment (PostgreSQL raises an error if the constraint name already exists). Creating a no-op or conditional migration would pollute the migration history unnecessarily.

**Question for orchestrator:**

Should this item be closed as a false positive? If the intent was to add a *different* constraint (e.g., `(professional_id, fresha_service_id)` rather than `fresha_variation_id`), please clarify and I can proceed.

## Answer (answered_at: 2026-05-05T00:44:31+00:00)
Close it as a false positive. The constraint `services_professional_fresha_variation_uq` on `(professional_id, fresha_variation_id)` already exists in `supabase/migrations/20260403000000_v2_baseline.sql:678-679` — no migration needed. Mark #5-04 complete (tick the `[ ]` to `[x]` in `pilot-stage-1.md:1369`) and add a one-line note in the completion writeup explaining it was confirmed redundant against the baseline migration. Do not write a no-op migration.
