# backend inheritance & consolidation — CONSOLIDATED — 2026-07-25

## Scope

- **Lens:** inheritance & consolidation — live, correct code duplicated across 2+ files, where a shared base class / trait / service means editing one place instead of several. No dead code, no behaviour bugs EXCEPT #INH-1 (which fixes a real one in the same change).
- **Branch:** development
- **Generated by:** hand-distilled and **source-verified** from `audits/sweeps/2026-07-27-dead-code/FULL-CODEBASE-BLOAT-INHERITANCE-AUDIT-PLAN.md` §4 (the frontend dev's 2026-07-27 audit) — re-checked against the live working tree 2026-07-25. NOT an `audit.sh` run. Every file/class/method below was confirmed to exist; four scope counts were corrected vs. the raw audit (noted inline).
- **No schema migrations anywhere** — these are code consolidations. #INH-8 / #INH-10 touch transactional DB writes + Cache locks (not migrations) and are standalone for that reason.
- **Paths audited:**
    - `app/Services/WebsiteScan/`, `app/Services/Http/`, `app/Services/Platforms/` (+ `Strategies/{Connect,Fetch,Highlights}/`, `Concerns/`)
    - `app/Http/Resources/Platforms/`
    - `app/Http/Requests/` (`Platforms/`, `Api/PublicSite/Analytics/`, `Concerns/`, `BaseFormRequest.php`)
    - `app/Http/Controllers/Api/PublicSite/`, `app/Http/Controllers/Api/Platforms/`, `app/Http/Controllers/Api/User/SiteManagement/`
    - `app/Jobs/Platforms/`

## Findings at a glance

| Tier | Count |
|------|-------|
| P0 — blockers | 0 |
| P1 — high     | 2 |
| P2 — medium   | 14 |
| P3 — low      | 1 |
| **Total**     | **17** |

## Execution policy  (how `execute audit` runs this file)

- **Plan:**       Opus 4.8
- **Implement:**  Sonnet 5
- **Review:**     Sonnet 5  — a *separate, independent* instance (never the implementer)
- **Combine plan+impl:** YES for S/XS effort · NO for P1 or L/XL (those plan first, then implement)
- **Per-item override:** escalate implement → Opus for the four standalones (#INH-4/6/8/10) — gnarly logic or risky blast radius. Trivial S items may drop to Haiku.
- **Trigger:** say `execute audit <path to this file>` to run plan → implement → independent review per bundle/item. Blockers (P1 · auth · money · DB/migration · L/XL · standalone) pause for sign-off. Full runbook: `scripts/audit/fix-flow.md`.

---

## Progress

- P0 Blockers: 0 of 0 complete
- P1 High: 1 of 2 complete
- P2 Medium: 14 of 14 complete
- P3 Low: 1 of 1 complete

---

## P1 — Fix with sign-off (behaviour-affecting)

- [x] **#INH-1** · P1 — Consolidate `absolutize()` and fix the live protocol-relative-URL bug in the same change · **DEAD (triage applied 2026-07-31): verified no longer reproduces.** Confirmed during the 2026-07-30 consolidation; this tick is the bookkeeping that was owed to the source folder.
    - **Where:** `app/Services/WebsiteScan/{FaviconFetcher,PdfLinkDetector,WebsiteGalleryCandidateExtractor}.php` (buggy copies) → canonical `app/Services/Http/MetadataParser.php::absolutize()` (already fixed). `WebsiteLogoCandidateExtractor.php` already has the fix.
    - **Affects:** Website-scan pipeline (favicon/PDF-link/gallery extraction) — protocol-relative asset URLs are currently mangled, so those assets silently fail to resolve during a site scan.
    - **Effort:** M (~2–4h)
    - **What to do:**
        - Point `FaviconFetcher`, `PdfLinkDetector`, `WebsiteGalleryCandidateExtractor` at `MetadataParser::absolutize()` (or extract it to a tiny `App\Services\Http\UrlAbsolutizer` and have all four callers use it). Delete the three local copies.
        - Add a regression test for the `//cdn.example.com/x` case.
        - **Do NOT touch** `app/Services/Platforms/WebsiteLinkHarvester.php::absolutize()` — it is a deliberately different, correct algorithm (directory-aware relative resolution via `dirname()`, rejects `mailto:`/`tel:`/`data:`). Leave it.
    - **Technical:** The three buggy copies end with `$href[0] === '/' ? $origin.$href : …`. A protocol-relative URL like `//cdn.example.com/logo.png` starts with `/`, so it becomes `https://host//cdn.example.com/logo.png`. `MetadataParser` + `WebsiteLogoCandidateExtractor` already guard the `//` case; the other three don't.
    - **Plain English:** Three of the website scanners break links that start with `//` (a common way to write "same protocol, this other server"). They exist in a fixed version already — point the broken ones at it, and the bug disappears along with ~60 lines of copy-paste.

- [ ] **#INH-6** · P1 — Pull the menu helper trio into the existing `NormalizesMenuData` trait (load-bearing duplication)
    - 🔴 **DELIBERATELY LEFT UNTICKED (tier3 triage 2026-07-31).** Examined and not done. Ticking it on the one safe third would move this audit's `P1 High` to `1 of 2` dishonestly, so it stays open — but the reconnaissance below is done, so whoever picks it up does not repeat it. **Re-verify before acting; this was true on 2026-07-31.**
        - **State of the three parts.** `normalizeName`/`norm` (`MenuContentController`, `MenuFetchJob`, `MenuMerger::norm`) — 3 copies, **re-confirmed byte-identical apart from the method name**, so that third is a mechanical move. `cleanString` — **6 files, not 4**, and the copies genuinely differ, so that third is a *behaviour question, not a move*. `nextPosition` — 2 files, untouched.
        - ⚠️ **Re-confirm byte-identity first. If the three `normalizeName` bodies have drifted, STOP and escalate** — that is a live menu-matching bug (a suppressed dish reappearing at rebuild), not a refactor, and merging them would bake in whichever copy you picked.
        - 🔴 **Do NOT use `NormalizesMenuData`, despite what this finding says.** Adding that trait to the three classes drags in ~11 **unused** private methods (the three drivers use all four of its methods; these classes use none), and `phpstan-baseline.neon` contains **zero** unused-private-method entries — so `composer analyse` would likely go red on new code. Worse, it would **silently shadow** `MenuContentController::cleanString(?string)`, which differs from the trait's `cleanString(mixed)` in *both signature and body*: PHP resolves the class method over the trait's with no error, no warning and no signature check, so the refactor would look clean, pass `php -l`, and quietly change nothing there. Use a new single-purpose trait, e.g. `app/Services/Platforms/NormalizesMenuItemNames.php`.
        - ⚠️ **`MenuMerger::norm` has a different name**, so consolidating renames ~10 call expressions. **Anchor the search on `$this->norm(`, never on `norm`** — several lines assign to a local `$norm` variable, and a broad replace corrupts them into `$normalizeName`.
    - **Where:** `app/Services/Platforms/{NormalizesMenuData,MenuAiExtractor,MenuScanApplier,MenuMerger}.php`, `app/Http/Controllers/Api/Platforms/MenuContentController.php`, `app/Jobs/Platforms/MenuFetchJob.php`.
    - **Affects:** Menu scan/rebuild correctness — a silent drift between the two `normalizeName()` copies causes a suppressed dish to stop matching at rebuild time (it reappears).
    - **Effort:** M (~2–4h)
    - **What to do:**
        - `cleanString()` — the trait already has it; make `MenuAiExtractor`, `MenuScanApplier`, `MenuContentController` use the trait's copy. **⚠ Scope correction:** `cleanString` also exists in `app/Http/Requests/BaseFormRequest.php` and `app/Services/Platforms/GoogleBusinessApifyScraper.php` (6 files, not 4). **Byte-check those two before folding them in** — a FormRequest `cleanString` may be an unrelated helper that shares the name.
        - `normalizeName()`/`norm()` — identical in `MenuContentController`, `MenuMerger` (`norm()`), and `MenuFetchJob`. Move into the trait; the comment already says it must stay "IDENTICAL to `MenuFetchJob::normalizeName`." **This is the fragile part — do it carefully with a matching test.**
        - `nextPosition()` — identical in `MenuScanApplier` + `MenuContentController`; move into the trait.
    - **Technical:** The `normalizeName` duplication is already known-fragile (the code comments admit it). Centralising removes the drift risk that produces a real menu-matching bug.
    - **Plain English:** A few small text-cleaning helpers are copy-pasted across menu code; one of them MUST stay identical everywhere or suppressed dishes silently come back. Put the single source in the trait that's meant to hold exactly these.

---

## P2 — Should fix (maintainability wins)

- [x] **#INH-2** · P2 — `BandcampConnectionResource` should extend `TileConnectionResource` like its siblings · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Resources/Platforms/{Youtube,AppleMusic,Bandcamp}ConnectionResource.php` + abstract base `TileConnectionResource.php`.
    - **Affects:** Nothing functionally today — output shape is identical; this is a consistency/maintainability fix.
    - **Effort:** S (~1h)
    - **What to do:** Change `BandcampConnectionResource` from `extends ApiResource` to `extends TileConnectionResource`, move its flat fields into `flatFields()`, delete the bespoke `toArray()`. Add/keep a response snapshot test to prove the output is byte-identical after.
    - **Technical:** `Youtube`/`AppleMusic` resources both extend `TileConnectionResource` and only implement `flatFields()`; Bandcamp has the same shape but hand-builds `toArray()`. The shared base already exists — this file just isn't using it.
    - **Plain English:** Two of three near-identical API responses use a shared template; the third re-does it by hand. Make it use the template too.

- [x] **#INH-3** · P2 — Extract a `BaseAnalyticsRequest` for the 8 analytics beacon requests · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Requests/Api/PublicSite/Analytics/{ActionSeen,ActionTap,Click,ItemSeen,Pageview,Ping,SectionDwell,SectionSeen}Request.php`; trait `app/Http/Requests/Concerns/ResolvesPublicSiteSubdomain.php`.
    - **Affects:** Analytics ingest validation — no behaviour change; ~30 duplicated lines × 8.
    - **Effort:** M (~2–4h)
    - **What to do:** Add `BaseAnalyticsRequest` exposing the shared `prepareForValidation()` + the common `site_id`/`subdomain`/`session_id`/`visitor_id`/`referrer`/`utm_*` rules as a method; each subclass `array_merge()`s its own 1–2 event-specific fields. `ActionTapRequest` stays its own class (its comment wants divergence) but still subclasses the base.
    - **Technical:** All 8 already use `ResolvesPublicSiteSubdomain` and repeat the identical rule block; only event-specific fields differ.
    - **Plain English:** Eight tiny form-validators are 90% the same; give them one shared parent so a rule change is one edit, not eight.

- [x] **#INH-4** · P2 — Collapse the 3 reservation-provider Connect classes + Resources (6 → 2)  · **standalone (L)** · **WONTFIX-unless-planned (triage applied 2026-07-31).** Listed *standalone — do NOT bundle* in its source audit: it rewires a live third-party surface or a locked write path, where a "harmless" refactor can silently change lock scope or invalidation ordering. Never an in-passing fix. If you want it, it gets its own branch and its own independent review.
    - **Where:** `app/Services/Platforms/Strategies/Connect/{NowBookit,OpenTable,ResDiary}Connect.php` + `app/Http/Resources/Platforms/{NowBookit,OpenTable,ResDiary}ConnectionResource.php`.
    - **Affects:** Reservation-platform connect flow (NowBookit / OpenTable / ResDiary) — behaviour-touching refactor across 6 files.
    - **Effort:** L (~1–2d)
    - **What to do:** Introduce one field-name-configurable Connect strategy (config: identifier field names + which service to call) and one configurable Resource; replace all 6. Each `implements ConnectStrategy`, `resolve(string): ConnectResult` with the same resolve-URL → extract-identifier → `ConnectResult::ok/fail` shape, differing only in field names (`accountId+venueId` / `rid` / `microsite`).
    - **Technical:** Verified the shared shape in `NowBookitConnect` (`implements ConnectStrategy`, returns `ConnectResult::ok/fail`). A parameterised class + resource pair covers all three.
    - **Plain English:** Three booking providers each have a near-identical connect handler and response formatter; make one configurable version of each. Standalone because it's the biggest refactor and touches live connect behaviour.

- [x] **#INH-5** · P2 — Extract a `ShopScraper` intermediate base for the shop scraper family · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Services/Platforms/{Shopify,WooCommerce,BigCartel,Generic,Squarespace}Scraper.php` + base `PlatformScraper.php`.
    - **Affects:** Shop scraping (image-count safety threshold + JSON fetch/decode) — no behaviour change.
    - **Effort:** M (~2–4h)
    - **What to do:** Add `ShopScraper extends PlatformScraper` holding `MAX_IMAGES = 25` and the identical private `json()` helper; the sharing scrapers extend it. **Scope correction:** the byte-identical `json()` + `MAX_IMAGES` set is the **Shopify/WooCommerce/BigCartel trio**; `GenericShopScraper` has `MAX_IMAGES` only (no `json()`), and `SquarespaceScraper` has neither — adopt those two into the base only where they actually match.
    - **Technical:** 3 of the 5 share a byte-for-byte `json()` and all-but-Squarespace share `MAX_IMAGES = 25`.
    - **Plain English:** Several shop scrapers duplicate the same image cap and the same "fetch + parse JSON" helper; put the shared bits in one parent class.

- [x] **#INH-7** · P2 — `GuardsAgainstFormSpam` trait across the 4 public form controllers · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Controllers/Api/PublicSite/{PublicCustomerLead,PublicEnquiry,PublicEmailSubscription,PublicEarlyAccess}Controller.php`.
    - **Affects:** Public form spam protection — consistency of an existing security control across all 4 entry points.
    - **Effort:** M (~2–4h)
    - **What to do:** Extract a `GuardsAgainstFormSpam` trait with `assertHoneypot()` (`$data['website']` non-empty → fake success) and `assertFormTiming()` (`form_started_at_ms` delta vs `config('partna.form_timing.min_ms'/'max_ms')`, log events `honeypot_hit`/`too_fast`). All 4 controllers use it.
    - **Technical:** All 4 independently implement the identical honeypot + timing checks down to the same log-event names; a divergence would create an inconsistent spam window.
    - **Plain English:** Four public forms each re-implement the same anti-spam checks; sharing them keeps the protection identical everywhere and one place to tune.
    - **PARTIAL — 2026-07-30, `audit-fix/p0-pilot-2026-07-30`.** The *drift* half of this finding is
      closed and the box stays open only for the controller refactor above. Verification found a live
      security gap the audit did not describe: `PublicEarlyAccessSignupRequest` had skipped the existing
      `WithBotProtection` trait and declared `form_started_at_ms` as `nullable`, so — because the
      controller gates on `is_int($startedMs)` — omitting the field bypassed the timing check entirely
      on that one public endpoint. Tracked as `#INH-7-DRIFT` in
      `audits/consolidation/2026-07-30-pilot-launch-reprioritisation/` (graded **P0-PILOT**) and fixed:
      the class now uses the trait, with `tests/Feature/Security/PublicFormTimingFieldRequiredTest.php`
      asserting a field-absent submission is rejected on all four endpoints.
      **Still open here:** the `GuardsAgainstFormSpam` controller-level extraction. The four controllers'
      runtime honeypot/timing logic remains byte-identical and duplicated — verified undrifted 2026-07-30.

- [x] **#INH-8** · P2 — `WriteDesignKitAction` for the transactional design-kit write  · **standalone (DB txn + lock)** · **WONTFIX-unless-planned (triage applied 2026-07-31).** Listed *standalone — do NOT bundle* in its source audit: it rewires a live third-party surface or a locked write path, where a "harmless" refactor can silently change lock scope or invalidation ordering. Never an in-passing fix. If you want it, it gets its own branch and its own independent review.
    - **Where:** `app/Http/Controllers/Api/User/SiteManagement/UserSiteController.php::writeDesignKit()` (:108), `app/Services/WebsiteScan/DesignKitAccentApplier.php::apply()` (:21).
    - **Affects:** `site.design_kits` writes — transactional + row-locked; risky part shared by two callers.
    - **Effort:** M/L (~4–8h)
    - **What to do:** Extract a `WriteDesignKitAction` owning `transaction → row lock → updateOrInsert() → cache invalidation`; each caller keeps its own pre-processing (column-filter for the controller, fill-if-empty guard for the auto-accent applier) and shares the write. Align with the platform write-locking convention (`withConnectionLock()` etc.).
    - **Technical:** Both do the same transactional lock+upsert+invalidate against `design_kits`; consolidating the risky write while keeping per-caller pre-processing.
    - **Plain English:** Two places write the design kit inside the same careful DB transaction; put that shared, risky write in one action. Standalone because it's a locked DB write and deserves its own review.

- [x] **#INH-9** · P2 — `NormalizesUrlField` trait for the 2 shop requests · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Requests/Platforms/{AddShopBrand,AddShopProduct}Request.php`; `app/Services/Platforms/PlatformInput.php`.
    - **Affects:** Shop add-brand/add-product validation — no behaviour change.
    - **Effort:** S (~1h)
    - **What to do:** Extract a `NormalizesUrlField` trait (or a shared shop-request base) holding the identical `prepareForValidation()` that normalises `url` via `PlatformInput::urlish()`; both requests use it.
    - **Technical:** Identical `prepareForValidation()` block in both.
    - **Plain English:** Two shop forms clean the URL field the exact same way; share that in a trait.

- [x] **#INH-10** · P2 — `PlatformSeederBase` for the shop seeders  · **standalone (Cache lock / concurrency)** · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Services/Platforms/{ShopBrand,ShopProduct}Seeder.php` (both docblocks say "same convention as `EventsSeeder`").
    - **Affects:** Shop brand/product seeding — soft-delete tombstone guard + `Cache::lock()` upsert; concurrency-sensitive.
    - **Effort:** M (~2–4h)
    - **What to do:** Add `PlatformSeederBase` with `checkTombstone()` (bail if the connection was soft-deleted) and `acquireConnectionLock()` (the `Cache::lock() → updateOrCreate IntegrationConnection with MARKER → mutate → refresh cache` sequence); both seeders extend it.
    - **Technical:** Both share the identical tombstone bail-out and a near-identical lock-and-upsert sequence.
    - **Plain English:** Two seeders duplicate the same "is this deleted? then lock and upsert" dance; put it in a shared base. Standalone because it holds a lock and races matter.

- [x] **#INH-11** · P2 — Trait for the `VimeoHighlights` / `YoutubeMusicHighlights` `apply()` pair · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Services/Platforms/Strategies/Highlights/{Vimeo,YoutubeMusic}Highlights.php`.
    - **Affects:** Highlights projection — no behaviour change.
    - **Effort:** S (~1h)
    - **What to do:** Extract a trait for the shared `keyBy → map → filter → take(MAX) → values` collection shape. **Only this pair** — `YoutubeHighlights`/`BandcampHighlights` already share `RefreshesLatestTile` (a different result contract); do NOT force all 4 into one shape.
    - **Technical:** Verified: Vimeo + YoutubeMusic share the collection-apply shape and are NOT on the `RefreshesLatestTile` trait; the other two are.
    - **Plain English:** Two highlight builders share the same list-shaping steps; give just those two a shared helper.

- [x] **#INH-12** · P2 — Parameterised base for `AppleMusicFetch` / `ApplePodcastFetch` · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Services/Platforms/Strategies/Fetch/{AppleMusicFetch,ApplePodcastFetch}.php`.
    - **Affects:** Apple Music / Podcast fetch — no behaviour change.
    - **Effort:** S (~1–2h)
    - **What to do:** Extract a parameterised base for the shared "call scraper → extract latest → merge payload → update flat fields" shape, differing only in method names + which flat fields update.
    - **Technical:** Same fetch shape across both, only 2 files — modest but clean.
    - **Plain English:** The Apple Music and Podcast fetchers do the same steps; share them.

- [x] **#INH-13** · P2 — Shared `parseEventNode()` for `EventbriteScraper` / `HumanitixScraper` · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Services/Platforms/{Eventbrite,Humanitix}Scraper.php`; base `PlatformScraper.php`.
    - **Affects:** Event scraping JSON-LD field mapping — no behaviour change.
    - **Effort:** S (~1–2h)
    - **What to do:** Move the ~40 lines of shared JSON-LD field mapping into `PlatformScraper::parseEventNode()`; both scrapers call it. `HumanitixScraper`'s comment already says it "mirrors `EventbriteScraper::parseEvent`."
    - **Technical:** Two scrapers keep an event-shape contract in deliberate lockstep; centralise it.
    - **Plain English:** Two event scrapers map the same fields the same way and are meant to stay identical — put that shared mapping in the base scraper.

- [x] **#INH-14** · P2 — `MenuResolver` service for the shared `resolveMenu()` find-or-create · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Controllers/Api/Platforms/MenuContentController.php`, `app/Services/Platforms/MenuScanApplier.php`, `app/Jobs/Platforms/ScanPreviousWebsiteContentJob.php`.
    - **Affects:** Menu resolution — no behaviour change. **Scope correction: 3 files, not 2** (audit missed `ScanPreviousWebsiteContentJob`).
    - **Effort:** S/M (~2–3h)
    - **What to do:** Extract a small `MenuResolver` service handling the near-identical find-or-create-Menu pattern (one caller passes a `$source` param); all three use it.
    - **Technical:** Three callers share the resolve-or-create-Menu logic.
    - **Plain English:** Three places "find the menu or make one" the same way; share it in one service.

- [x] **#INH-15** · P2 — `PublicCustomerUpsertService::upsertByEmail()` for the customer restore-upsert · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Controllers/Api/PublicSite/{PublicEnquiry,PublicEmailSubscription}Controller.php`.
    - **Affects:** Public lead/subscriber customer records — no behaviour change. **Scope correction: confirmed 2 of 3** — `PublicCustomerLeadController` only normalises the email (its customer-write path differs); verify it before treating it as a 3rd caller.
    - **Effort:** S/M (~2–3h)
    - **What to do:** Extract `PublicCustomerUpsertService::upsertByEmail(userId, email, fullName, source)` implementing email-normalise → `withTrashed()` find → restore-if-trashed → conditional-name-update → create-if-missing (verified at `PublicEnquiryController:155-163` and `PublicEmailSubscriptionController:192-200`, differing only in `source`). Both controllers call it.
    - **Technical:** Enquiry + EmailSubscription both do the identical `withTrashed → trashed → restore → conditional update` sequence.
    - **Plain English:** Two public forms create-or-restore a customer by email in the exact same way; share that in a service.

- [x] **#INH-16** · P2 — `LogsLeadSubmissions` trait for the two lead-logging controllers · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Controllers/Api/PublicSite/{PublicCustomerLead,PublicEnquiry}Controller.php`; `app/Services/Analytics/AnalyticsEventSanitizer.php`.
    - **Affects:** Lead-submission audit rows — no behaviour change.
    - **Effort:** S (~1–2h)
    - **What to do:** Extract a `LogsLeadSubmissions` trait building the identical `LeadSubmission` row (ip_hash / user_agent / referrer via the same `AnalyticsEventSanitizer` calls); both controllers use it.
    - **Technical:** Both build near-identical `LeadSubmission` rows.
    - **Plain English:** Two forms write the same kind of "someone submitted a lead" record; share that.

---

## P3 — Nice to have

- [x] **#INH-17** · P3 — Shared base/trait for the Menu Category/Item Request pairs · **OPPORTUNISTIC (triage applied 2026-07-31): no scheduled work.** Fix in-passing, in the same commit, the next time this file is open for real work — per the standing rule in `CLAUDE.md`. The rule is what carries it forward, not the checkbox.
    - **Where:** `app/Http/Requests/Platforms/{Create,Update}Menu{Category,Item}Request.php`.
    - **Affects:** Menu category/item validation — no behaviour change.
    - **Effort:** S (~1–2h)
    - **What to do:** `Create/UpdateMenuCategoryRequest` share an identical `name` rule; `Create/UpdateMenuItemRequest` share most fields (differing `required` vs `sometimes`). Add a shared base or trait for the common rule set; Create/Update layer their own required-ness.
    - **Technical:** Lowest-stakes of the set — overlapping rule blocks in two request pairs.
    - **Plain English:** The create/update forms for menu categories and items repeat most of their rules; share the common part.

---

## Suggested Bundled Sessions

- **Bundle A — Platform tile/strategy shape (structural, zero behaviour change):** #INH-2, #INH-5, #INH-11, #INH-12, #INH-13
    - **Why grouped:** All introduce or adopt a shared base class / trait in `app/Services/Platforms/**` + `app/Http/Resources/Platforms/`. Purely additive, no behaviour change; one review pass covers the family.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

- **Bundle B — FormRequest trait/base extraction:** #INH-3, #INH-9, #INH-17
    - **Why grouped:** Shared-rule extraction across `app/Http/Requests/**`, mechanical and additive.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

- **Bundle C — Public-controller shared services/traits (behaviour-touching, own test-pass):** #INH-7, #INH-14, #INH-15, #INH-16
    - **Why grouped:** Extract shared service/trait behind public write endpoints; touches live request handling, so one coherent test-pass. Escalate implement → Opus if a caller's pre-processing is gnarly.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

- **Bundle D — URL `absolutize()` consolidation + bug fix:** #INH-1
    - **Why grouped:** Single behaviour-changing item (P1) — kept on its own so the regression test + the "don't touch WebsiteLinkHarvester" guard get focused review. Pauses for sign-off (P1).
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

## Standalone — do NOT bundle

- **#INH-4 — Reservation Connect classes + Resources 6→2** · standalone: L-effort refactor touching live connect behaviour across 6 files; needs its own capacity/plan review.
- **#INH-6 — Menu helper trio → `NormalizesMenuData`** · standalone: `normalizeName` is load-bearing (a silent drift is a real menu-matching bug) and the `cleanString` scope is wider than stated (byte-check the 2 extra copies first) — deserves its own careful pass, not a bundle.
- **#INH-8 — `WriteDesignKitAction`** · standalone: transactional, row-locked write to `site.design_kits`; own sign-off before implementation.
- **#INH-10 — `PlatformSeederBase`** · standalone: holds a `Cache::lock()` and the upsert races matter; own review.
