# security bundle audit — CONSOLIDATED — 2026-07-20

## Scope

- **Lens:** bundle 'security' — security/auth/tenant-isolation/IDOR/injection/SSRF/PII/MFA (SEC-*), schema & RLS / search_path DB-layer tenant isolation (SCHEMA-*), configuration hygiene & secret leakage / CORS (CFG-*), edge worker origin-trust & KV poisoning (EDGE-*), and privacy & data-rights compliance / PII inventory (PRIV-*) — the full security & tenant-isolation sweep across the whole backend
- **Branch:** chore/security-tooling
- **Generated by:** `scripts/audit/audit.sh` (DeepSeek scan + Claude adjudication)
- **Paths audited:**
    - `app/Http/Controllers/Api/Staff`

## Findings at a glance

| Tier | Count |
|------|-------|
| P0 — blockers | 0 |
| P1 — high     | 0 |
| P2 — medium   | 7 |
| P3 — low      | 5 |
| **Total**     | **12** |

## Execution policy  (how `execute audit` runs this file)

- **Plan:**       Opus 4.8
- **Implement:**  Sonnet 4.6
- **Review:**     Sonnet 4.6  — a *separate, independent* instance (never the implementer)
- **Combine plan+impl:** YES for S/XS effort · NO for P0/P1 or L/XL (those plan first, then implement)
- **Per-item override:** escalate to Opus for gnarly logic or risky blast radius; a trivial
  mechanical S item may drop to Haiku. Default to the table above unless an item clearly warrants a change.
- **Trigger:** say `execute audit <path to this file>` to run plan → implement → independent review
  per bundle/item. Blockers (P0 · auth · money · DB/migration · L/XL) pause for sign-off.
  Full runbook: `scripts/audit/fix-flow.md`.

---

# Security & Tenant-Isolation Sweep Audit — 2026-07-20

**Branch:** chore/security-tooling
**Lens:** Bundle `security` — 5 lens-focused scans: SEC-* (auth/tenant-isolation/IDOR/PII/MFA), SCHEMA-* (RLS/search_path/constraints/indexes), CFG-* (configuration hygiene/secret leakage/CORS), EDGE-* (Cloudflare Worker origin-trust/KV poisoning), PRIV-* (privacy/data-rights/PII inventory)
**Pipeline:** scan-tier draft by `deepseek-v4-pro`, adjudicated by `claude-sonnet-4.5`
**Source files audited:**
- app/Http/Controllers/Api/Staff/**/*.php (25 controllers across Analytics, EarlyAccess, FeatureAvailability, FeatureFlag, Feedback, Segments, StaffSite, UserSiteManagement)
- app/Policies/*.php (all 18 policy classes + `AppServiceProvider::boot()` policy registrations)
- routes/api/staff.php (both staff route groups — `staff` vs `staff.admin` middleware)
- supabase/migrations/20260526000000_baseline_standalone_user.sql, 20260527030000_rename_professional_to_user.sql, 20260527050000_rename_professional_constraints_indexes.sql
- app/Services/User/AccountDeletionService.php
- config/partna.php
- tests/Feature/Security/PolicyCoverageTest.php

**Coverage note — edge-worker lens produced zero findings, not a clean bill of health.** The `edge-worker` scan's draft is a scan-tier error message: none of its declared scope (`cloudflare-worker/src`, `app/Jobs/Cloudflare`, `app/Services/Cloudflare`, `app/Services/Cache/SiteCacheService.php`) was fed to that pass, so nothing was read and no EDGE-* findings exist to adjudicate. This is the exact "silent scope hole reads as clean" failure mode the audit pipeline's own integrity guard exists to catch — flagging here rather than passing it through silently. Re-run `edge-worker` with its correct `--scope` before treating Cloudflare Worker / KV-contract security as reviewed.

## Progress

- P0 Blockers: 0 of 0 complete
- P1 High: 0 of 0 complete
- P2 Medium: 0 of 7 complete
- P3 Low: 0 of 5 complete

---

## P2 — Should fix

- [ ] **#SEC-1** · P2 — StaffNotificationEmailPolicyController has zero Policy-layer authorization on all four methods
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffNotificationEmailPolicyController.php (`indexGlobal`, `updateGlobal`, `indexProfessional`, `updateProfessional`)
    - **Affects:** Global and per-professional notification email policy reads/writes.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Resolve `$staff = $request->attributes->get('partna_staff')` in all four methods.
        - Add `$this->authorizeForUser($staff, 'staffView', Notification::class)` to `indexGlobal()`/`indexProfessional()`.
        - Add `$this->authorizeForUser($staff, 'staffManage', Notification::class)` to `updateGlobal()`/`updateProfessional()` — `NotificationPolicy::staffManage(PartnaStaff $actor, ...)` already exists (`return $actor->isAdmin();`), used today by `StaffNotificationController::store`.
    - **Technical:** All four routes (`routes/api/staff.php:256-259`) sit inside the `staff.admin`-gated route group, so today only admin-role staff can reach this controller at all — there is no live privilege-escalation path. But every sibling write controller in the same route group (`StaffSegmentController`, `StaffEarlyAccessController`, `StaffFeatureAvailabilityController`) redundantly re-checks `staffManage`/`staffView` at the Policy layer specifically as insurance against the route-middleware group being misconfigured later — several of those controllers' own comments say so explicitly ("staff.admin middleware could be removed/misconfigured; this keeps the authorization co-located"). This controller is the one outlier that skips that layer entirely, using an already-registered `NotificationPolicy` that has the exact ability it needs sitting unused.
    - **Plain English:** Every other room in this part of the staff dashboard has both a building-level door lock (the route rule) and a room-level lock (a permission check) on top of it, in case the building lock is ever changed by mistake. This one room only has the building lock. Nobody can get in today, but there's no backup if that outer lock is ever loosened.
    - **Evidence:**
        ```php
        public function indexGlobal(): JsonResponse
        {
            $policies = DB::table('notifications.notification_email_policies')
                ->whereNull('user_id')
                ->get(['category_key', 'mode'])
                ->keyBy('category_key');
        ```
        ```php
        public function updateGlobal(UpdateNotificationEmailPoliciesRequest $request): JsonResponse
        {
            foreach ($request->validated()['policies'] as $update) {
                DB::statement(
        ```

- [ ] **#SEC-2** · P2 — StaffCustomerManagementController / StaffServiceManagementController / StaffServiceCategoryManagementController authorize the professional, not the staff actor
    - **Where:** app/Http/Controllers/Api/Staff/UserSiteManagement/StaffCustomerManagementController.php:64,78,89,98,107; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffServiceManagementController.php (`store`/`show`/`update`/`destroy`/`forceDestroy`/`restore`); app/Http/Controllers/Api/Staff/UserSiteManagement/StaffServiceCategoryManagementController.php (same set) — plus `reorder()`/`reorderLayout()` in the two Service* controllers, which have no Policy call at all
    - **Affects:** Customer, Service, and ServiceCategory CRUD for a professional's site.
    - **Effort:** M (~2–4h)
    - **What to do:**
        - Resolve `$staff = $request->attributes->get('partna_staff')` and switch each `$this->authorizeForUser($professional, 'view'|'update'|'delete'|'create', $resource)` call to `$this->authorizeForUser($staff, 'staffManage', $professional)`, mirroring the pattern already used by `StaffIntegrationManagementController` and `StaffLinkBlockManagementController::update/destroy` (`UserSelfPolicy::staffManage` is `$actor->isAdmin()` — already registered, no new Policy code needed).
        - Add the same call to `reorder()` / `reorderLayout()` on both Service controllers, which currently have zero authorization of any kind.
    - **Technical:** Correcting the overstatement in the original draft: this is **not** a cross-tenant IDOR — `Route::scopeBindings()` on the staff route group means a `{customer}`/`{service}`/`{category}` that doesn't belong to the route's `{professional}` 404s before the controller body ever runs (the controller's own comment confirms this: "route group already scopes via ->scopeBindings() and User::customers()"). And the mutating endpoints (`update`/`destroy`/`forceDestroy`/`reorder*`) are already restricted to `staff.admin`-role staff by route middleware (`routes/api/staff.php:195-225`), so there is no live privilege-escalation path today either. The real defect is that `CustomerPolicy`/`ServicePolicy::update`/`view` take `User $actor` and check `$resource->user_id === $actor->id` — passing `$professional` as `$actor` makes that check tautologically true (the professional always owns their own rows), so the Policy call contributes nothing beyond what scoping already guarantees. It silently drops the one thing a staff-actor Policy call *would* add: an admin-role check that isn't otherwise enforced at this layer, matching the established `staffManage`/`staffManageBlock` convention used elsewhere in the same file tree. The two `index`/`show` reads in these three controllers sit in the non-admin `staff` route group (any staff role), so for those specifically this is the only place a role distinction could be enforced — see #SEC-5, which covers that read-side gap for these same controllers.
    - **Plain English:** Three "edit a professional's stuff" screens ask a question, but they're asking it of the wrong person — they check "does this customer/service belong to this professional?" (always yes, since the system already only shows you your own professional's records) instead of "is this particular staff member allowed to make this change?" Today a second door (a building rule) already restricts these changes to senior staff, so nothing is actually exposed — but the intended second lock on this specific door doesn't work, so if the building rule is ever loosened, there's no backup.
    - **Evidence:**
        ```php
        public function show(Request $request, User $professional, Customer $customer): JsonResponse
        {
            // Defence in depth: route group already scopes via ->scopeBindings() and
            // User::customers(). ...
            $this->authorizeForUser($professional, 'view', $customer);
        ```
        ```php
        public function store(StaffStoreServiceRequest $request, User $professional): JsonResponse
        {
            $this->authorizeForUser($professional, 'create', new Service(['user_id' => $professional->id]));
        ```
        ```php
        public function reorder(StaffReorderServiceCategoryRequest $request, User $professional): JsonResponse
        {
            app(ReorderService::class)->reorder(
                $request->input('ids', []),
                ServiceCategory::query()->where('user_id', $professional->id),
                "service-categories:{$professional->id}",
            );

            return $this->success(['ok' => true]);
        }
        ```

- [ ] **#SEC-3** · P2 — StaffAccountDeletionController reads raw `input()` instead of `validated()`, and `initiate()`/`cancel()` have no Policy gate
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffAccountDeletionController.php:39-56, 68-84
    - **Affects:** Staff-initiated GDPR-flow account deletion/cancellation for a professional.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - `initiate()` already receives a `StaffInitiateDeletionRequest` FormRequest — switch `$request->input('reason')` / `$request->input('override_obligations', false)` to `$request->validated('reason')` / `$request->validated('override_obligations', false)`.
        - `cancel()` is typed as a plain `Illuminate\Http\Request`, not a FormRequest — it has no `->validated()` to call. Either add a small `StaffCancelDeletionRequest` FormRequest (validating `reason` as optional string), or leave `cancel()` on `->input()` with an explicit `$request->validate([...])` call before use. Do not blind-switch it to `->validated()` as originally proposed — that method doesn't exist on a plain `Request` and would fatal.
        - Add `$staff = $request->attributes->get('partna_staff')` + `$this->authorizeForUser($staff, 'staffManage', $professional)` to both `initiate()` and `cancel()`.
    - **Technical:** Correcting the original draft's PII-leak claim: `adminInitiate`/`adminCancel` only ever call `$request->ip()` and `$request->userAgent()` off the passed `$request` (verified in `AccountDeletionService::logAuditEvent`) — this is the intended fraud/impersonation trail, not an accidental data leak, and matches the same `ip`/`user_agent` capture pattern used by `StaffSiteManagementController::update`. The `show()` method's lack of an `authorizeForUser` call is also intentional, not a gap — its own doc comment states it's "Available to all staff (not just admin) so support can answer... questions without elevated privileges," consistent with the read-side design used throughout this route group (see #SEC-5). What remains real: `initiate`/`cancel` sit in the `staff.admin`-gated route group already, so — as with #SEC-1/#SEC-2 — there's no live bypass, but the codebase's own defense-in-depth convention (Policy check on top of admin-only middleware for destructive account actions) is broken here specifically, and the `input()` vs `validated()` gap means a non-conforming `reason`/`override_obligations` value could reach the service layer without going through the FormRequest's cast/trim rules.
    - **Plain English:** When a staff member starts or cancels a professional's account deletion, the code reads the raw form fields instead of the cleaned-up, checked version — and it never double-checks that the staff member has permission to do this specific action, relying only on the outer building lock. Nobody can exploit this today, but a routine, sensitive, hard-to-undo action (deleting someone's account) deserves its own explicit permission check, not just a hallway door.
    - **Evidence:**
        ```php
        $result = $this->deletionService->adminInitiate(
            professional: $professional,
            staffActorId: (string) $staff->id,
            staffActorHandle: (string) ($staff->name ?? $staff->primary_email ?? ''),
            reason: (string) $request->input('reason'),
            overrideObligations: (bool) $request->input('override_obligations', false),
            request: $request,
        );
        ```

- [ ] **#SEC-4** · P2 — StaffLinkBlockManagementController::index()/store() lack authorization while update()/destroy()/reorder() have it
    - **Where:** app/Http/Controllers/Api/Staff/UserSiteManagement/StaffLinkBlockManagementController.php:28, 35
    - **Affects:** A professional's link-block list and creation, in the any-staff-role route group.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add `$staff = $request->attributes->get('partna_staff')` and `$this->authorizeForUser($staff, 'staffManageBlock', $professional)` to `index()` and `store()`, matching `update()`/`destroy()`/`reorder()` in the same file.
    - **Technical:** `update()`, `destroy()`, and `reorder()` correctly call `UserSelfPolicy::staffManageBlock` (`$actor->isAdmin()` + `denyIfPendingDeletion`). `index()` (routed in the non-`staff.admin` group, `routes/api/staff.php:115`, any staff role) and `store()` (routed in the `staff.admin` group, line 231) have no such call — `index()` is a genuine any-staff-role read gap (see the same root cause covered in #SEC-5); `store()` is already admin-gated by middleware, so its gap is the same redundant-defense-in-depth pattern as #SEC-1/#SEC-3.
    - **Plain English:** Three of five doors into this professional's link list have a proper lock; the "view all" and "add new" doors don't. Anyone with any staff badge can currently see the list without an extra check (though not change it, since creation is already gated elsewhere), and there's no seam to see who looked.
    - **Evidence:**
        ```php
        public function index(User $professional): JsonResponse
        {
            return $this->success([
                'blocks' => LinkBlockResource::collection(
                    $professional->linkBlocks()->orderBy('sort_order')->get()
                ),
            ]);
        }
        ```

- [ ] **#SEC-5** · P2 — Eleven read/write endpoints across eight controllers in the any-staff-role route group omit the Policy-layer role gate that group's own convention requires
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffAnalyticsController.php:31; app/Http/Controllers/Api/Staff/StaffSite/StaffEnquiryController.php:21; app/Http/Controllers/Api/Staff/StaffSite/StaffSiteController.php:17,25; app/Http/Controllers/Api/Staff/StaffSite/StaffWorkplaceController.php:18; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffDataExportController.php:31; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffSiteManagementController.php:21; app/Http/Controllers/Api/Staff/StaffSite/StaffSectionManagementController.php:32; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffCustomerManagementController.php:24; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffServiceManagementController.php:19; app/Http/Controllers/Api/Staff/UserSiteManagement/StaffServiceCategoryManagementController.php:18; app/Http/Controllers/Api/Staff/StaffSite/StaffNotificationController.php:143-151, 121
    - **Affects:** Staff-dashboard reads of analytics, enquiries, site data, workplace data, section-block listings, customer/service/category listings, and in-app notification history.
    - **Effort:** M (~2–4h)
    - **What to do:**
        - Add `$staff = $request->attributes->get('partna_staff')` and `$this->authorizeForUser($staff, 'staffView', $professional)` (or `Notification::class` for the two global-scope notification reads) to each listed method — `UserSelfPolicy::staffView` is already registered and used this way by `StaffIntegrationManagementController`/`StaffUserController::show`.
    - **Technical:** `routes/api/staff.php:143-145` documents the convention directly: "staff-dashboard read surface (any staff role; policies add the role gate — UserSegmentPolicy / EarlyAccessSignupPolicy / FeatureAvailabilityPolicy ::staffView)." `StaffSegmentController`, `StaffEarlyAccessController`, `StaffFeatureAvailabilityController`, and `StaffAggregateAnalyticsController` all correctly implement that convention in the same route group (confirmed in source). The eleven endpoints above sit in the identical group and skip it entirely — a real, systemic inconsistency, not a hypothetical one. Its live impact is bounded today: `UserSelfPolicy::staffView` is currently a no-op (`return true;`, by design — the file's own comment says the PII gate lives in `UserStaffResource`'s `$showPii`, not here), so wiring it in doesn't block anyone yet; its value is closing the audit-trail/consistency gap and creating the seam the codebase's own doctrine expects for future tightening. `StaffDataExportController::store`'s `sendTo=staff` branch already has a real (if inline, not Policy-based) `$staff->isAdmin()` check; only its default `sendTo=professional` path and the read endpoints above are fully ungated.
    - **Plain English:** This whole wing of the staff dashboard was built with a rule: "any staff badge gets past the front door, but each room adds its own sign-in sheet." Eleven rooms across eight files never installed that sign-in sheet — support staff and admins alike walk in the same way today (nothing currently distinguishes them at this layer), but there's no record of who looked at what, and no way to later say "only admins can see this room" without writing new code from scratch.
    - **Evidence:**
        ```php
        public function summary(Request $request, User $professional): JsonResponse
        {
            $days = (int) $request->query('days', 30);
            $days = max(1, min(365, $days));
        ```
        ```php
        public function index(Request $request, User $professional): JsonResponse
        {
            $page = Enquiry::query()
                ->where('user_id', $professional->id)
        ```
        ```php
        public function show(string $subdomain): JsonResponse
        {
            $row = AllSiteData::query()
                ->whereRaw('lower(subdomain) = lower(?)', [$subdomain])
        ```
        ```php
        public function show(User $professional): JsonResponse
        {
            $site = Site::query()
                ->where('user_id', $professional->id)
                ->first();
        ```
        ```php
        public function update(StaffUpdateSiteRequest $request, User $professional, UpdateSiteAction $action): JsonResponse
        {
            /** @var PartnaStaff|null $staff */
            $staff = $request->attributes->get('partna_staff');

            $site = $action->execute(
        ```
        ```php
        public function index(User $professional): JsonResponse
        {
            // Return ALL section blocks (active + inactive) so staff can toggle
            $sections = Block::query()
                ->where('user_id', $professional->id)
        ```
        ```php
        public function index(Request $request, User $professional): JsonResponse
        {
            $perPage = $this->normalizePerPage($request, (int) config('partna.staff.pagination.per_page', 25), (int) config('partna.staff.pagination.per_page_max', 100));
        ```
        ```php
        public function index(Request $request): JsonResponse
        {
            $limit = max(1, min((int) $request->query('limit', 50), 200));

            $query = Notification::query()->orderByDesc('created_at')->limit($limit);
        ```

- [ ] **#PRIV-1** · P2 — StaffWorkplaceController returns phone and address to all staff, no admin PII gate
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffWorkplaceController.php:45-65
    - **Affects:** All authenticated staff members; a professional's workplace phone number and full address.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Resolve the staff actor and gate `phone`/`address`/`address_line1`/`city`/`state`/`postcode`/`country`/`latitude`/`longitude` on `$staff->isAdmin()`, mirroring `StaffUserController`'s `$showPii` pattern.
        - Return `null`/omit these keys for non-admin staff.
    - **Technical:** `normalizeProfile()` unconditionally includes every address/contact field regardless of the requesting staff member's role, and the route (`routes/api/staff.php:141`) sits in the non-admin `staff` group — any authenticated, MFA'd staff member sees this data today, with no equivalent to the `showPii` gate `StaffUserController` already applies to comparable fields on the `User` model itself.
    - **Plain English:** Any support-team member looking up a professional's workplace card can currently see their private phone number and home/business address, even if all they needed was the workplace's name. The platform already has a pattern for "only senior staff see contact details" elsewhere — this screen just never got it applied.
    - **Evidence:**
        ```php
        private function normalizeProfile(?Workplace $workplace): ?array
        {
            // ...
            return [
                'name' => $name,
                'address' => $this->trimOrNull($workplace->address),
                'address_line1' => $this->trimOrNull($workplace->address_line1),
                'city' => $this->trimOrNull($workplace->city),
                'state' => $this->trimOrNull($workplace->state),
                'postcode' => $this->trimOrNull($workplace->postcode),
                'country' => $this->trimOrNull($workplace->country),
                'latitude' => $workplace->latitude !== null ? (float) $workplace->latitude : null,
                'longitude' => $workplace->longitude !== null ? (float) $workplace->longitude : null,
                'phone' => $this->trimOrNull($workplace->phone),
                'website' => $this->trimOrNull($workplace->website),
            ];
        }
        ```

- [ ] **#PRIV-2** · P2 — Staff email-subscriber list and CSV export expose third-party PII to any staff member, no admin role gate
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffEmailSubscriberController.php:29-77 (`index`), 80-130 (`export`)
    - **Affects:** All authenticated staff members; a professional's newsletter/marketing subscriber list — third-party emails and full names, not the professional's own data.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Resolve `$staff = $request->attributes->get('partna_staff')` in both `index()` and `export()`.
        - Gate `export()` (bulk CSV download) on `$staff->isAdmin()` — return 403 for non-admin staff.
        - Consider whether `index()` (paginated list) also warrants the same gate, or a lighter `staffView` audit-trail call, given it exposes the same subscriber emails one page at a time.
    - **Technical:** The route group (`routes/api/staff.php:130-134`) is the non-admin `staff` group by design, and the controller's own doc comment says "Any-staff" — this is intentional, but unlike `StaffUserController`'s PII fields (which belong to the professional themselves), these rows belong to the professional's *customers/subscribers*, a third party who never consented to staff-wide visibility of their contact details. A bulk CSV export of third-party PII is a materially larger exposure than a single record view and warrants the same least-privilege treatment already applied elsewhere in the staff surface (`StaffUserController::$showPii`).
    - **Plain English:** Any support-team member can currently download a spreadsheet of every email address and name a professional's subscribers gave them, with no extra permission check — that's someone else's customer list, not the professional's own account details. Bulk downloads of other people's contact information should be reserved for staff who actually need that level of access.
    - **Evidence:**
        ```php
        /**
         * GET /staff/professionals/{professional}/email-subscribers/export
         * Any-staff. CSV stream matching the brand-side export verbatim.
         */
        public function export(Request $request, User $professional): StreamedResponse
        {
        ```

## P3 — Nice to have

- [ ] **#SEC-6** · P3 — StaffMeController::show() returns `supabase_uid` without a Policy gate; self-referential, low-value inconsistency
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffMeController.php:9-16
    - **Affects:** The authenticated staff member's own session context only.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Optionally add `$this->authorizeForUser($staff, 'staffView', $staff)` for consistency, or leave as-is with a short comment noting the resource IS the actor.
    - **Technical:** The resource returned is the caller's own record — there is no privilege-escalation path here regardless of whether a Policy call is added. This is a pure consistency nit against the "every staff endpoint calls `authorizeForUser`" convention, not a security gap.
    - **Plain English:** This screen shows a staff member their own badge details — there's no risk in that. Every other staff screen in the app runs an extra permission check first; this one is the sole exception, purely for tidiness.
    - **Evidence:**
        ```php
        public function show(Request $request)
        {
            return $this->success([
                'uid' => $request->attributes->get('supabase_uid'),
                'staff' => new PartnaStaffResource($request->attributes->get('partna_staff')),
            ]);
        }
        ```

- [ ] **#CFG-1** · P3 — Hardcoded cache TTL in StaffAggregateAnalyticsController
    - **Where:** app/Http/Controllers/Api/Staff/Analytics/StaffAggregateAnalyticsController.php:27
    - **Affects:** Staff aggregate-analytics endpoint cache freshness.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add a `config/partna.php` key (e.g. `staff.analytics_cache_ttl_seconds`, default 60) and replace the class constant with `config('partna.staff.analytics_cache_ttl_seconds', 60)`.
    - **Technical:** `config/partna.php` already carries a dense, established convention of externalized cache TTLs (`resolve_cache_ttl`, `cache_ttl_seconds`, `listing_cache_ttl_seconds`, the `cache.*` block at lines 1578-1584) — this hardcoded class constant is the outlier, not the norm, matching a pattern already fixed elsewhere in this file (see the `CFG-1` reference at `config/partna.php:1690` for `AnalyticsCacheService::summary()`).
    - **Plain English:** The "how often to refresh" number for this dashboard view is welded into the code instead of living in the settings file everything else uses — changing it later means a code deploy instead of flipping a setting.
    - **Evidence:**
        ```php
        private const CACHE_TTL_SECONDS = 60;
        ```

- [ ] **#CFG-2** · P3 — Hardcoded cache TTL in StaffStatsController
    - **Where:** app/Http/Controllers/Api/Staff/StaffSite/StaffStatsController.php:16
    - **Affects:** Staff ops-dashboard stats cache freshness.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add a config key (e.g. `staff.ops_stats_cache_ttl_seconds`, default 60) and reference it instead of the literal constant.
    - **Technical:** Same convention gap as #CFG-1 — one-off hardcoded value where the rest of the codebase externalizes TTLs to `config/partna.php`.
    - **Plain English:** Same idea as the analytics one above — this number is painted onto the code instead of living somewhere it can be tuned without a deploy.
    - **Evidence:**
        ```php
        private const CACHE_TTL_SECONDS = 60;
        ```

- [ ] **#CFG-3** · P3 — Magic number for user-agent truncation in StaffSiteManagementController
    - **Where:** app/Http/Controllers/Api/Staff/UserSiteManagement/StaffSiteManagementController.php:26
    - **Affects:** Audit-trail completeness for staff-initiated site renames.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add a config key (e.g. `staff.audit_user_agent_max_length`, default 1024) and use it in place of the literal `1024`.
    - **Technical:** Same category as #CFG-1/#CFG-2 — a tunable limit embedded directly in a controller rather than `config/partna.php`.
    - **Plain English:** The cutoff length for the browser-identifying text saved in the audit log is fixed in the code. If that limit ever needs to change, it currently requires a code deploy instead of a settings change.
    - **Evidence:**
        ```php
        'user_agent' => mb_substr((string) $request->userAgent(), 0, 1024),
        ```

- [ ] **#SCHEMA-1** · P3 — `core.users` staff search lacks `pg_trgm` indexes for `ILIKE '%term%'` queries
    - **Where:** app/Http/Controllers/Api/Staff/UserSiteManagement/StaffUserController.php:61-75
    - **Affects:** Staff professional-search performance, as the platform grows past pilot scale.
    - **Effort:** M (~2–4h)
    - **What to do:**
        - Enable the `pg_trgm` extension (not currently present anywhere in `supabase/migrations/`) and add GIN trigram indexes on `core.users.handle`, `display_name`, `primary_email`, `first_name`, `last_name`, `sector`, and `site.sites.subdomain`, via `CREATE INDEX CONCURRENTLY`.
    - **Technical:** Confirmed by grepping every migration file — `pg_trgm` is not enabled anywhere in this schema, and `StaffUserController::index`'s leading-wildcard `ILIKE '%term%'` search across seven columns cannot use a standard B-tree index. At pilot scale this is tolerable (small `core.users` row count); it will degrade linearly as the professional base grows.
    - **Plain English:** The staff search box scans the entire professional list, row by row, on every keystroke, because the database has no fast lookup for "contains this text anywhere." That's fine while the list is small; it will get noticeably slower as more people join the platform.
    - **Evidence:**
        ```php
        if ($searchLike) {
            $query->where(function ($qq) use ($searchLike) {
                $qq->whereRaw('handle ILIKE ?', [$searchLike])
                    ->orWhereRaw('display_name ILIKE ?', [$searchLike])
                    ->orWhereRaw('primary_email ILIKE ?', [$searchLike])
                    ->orWhereRaw('phone ILIKE ?', [$searchLike])
                    ->orWhereRaw('first_name ILIKE ?', [$searchLike])
                    ->orWhereRaw('last_name ILIKE ?', [$searchLike])
                    ->orWhereRaw('sector ILIKE ?', [$searchLike])
                    ->orWhereHas('site', function ($s) use ($searchLike) {
                        $s->whereRaw('subdomain ILIKE ?', [$searchLike]);
                    });
            });
        }
        ```

## Suggested Bundled Sessions

- **Bundle 1 — Config hygiene: hardcoded cache TTLs & magic numbers:** #CFG-1, #CFG-2, #CFG-3
    - **Why grouped:** Identical root cause (tunable constant belongs in `config/partna.php`) and identical mechanical fix across three unrelated files; no authorization or schema risk.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet (combine plan+impl given XS/S scope).

## Standalone — do NOT bundle

- **#SEC-1 — StaffNotificationEmailPolicyController missing Policy layer** · touches authorization.
- **#SEC-2 — Customer/Service/ServiceCategory wrong-actor authorization** · touches authorization; spans three controllers' Policy semantics.
- **#SEC-3 — StaffAccountDeletionController input/authz gaps** · touches authorization; GDPR account-deletion flow.
- **#SEC-4 — StaffLinkBlockManagementController missing authz** · touches authorization.
- **#SEC-5 — Eleven-endpoint staff read-surface Policy gap** · touches authorization; largest-blast-radius item, needs its own review pass.
- **#PRIV-1 — Workplace PII exposure** · touches authorization/PII-gating logic.
- **#PRIV-2 — Email-subscriber PII export exposure** · touches authorization/PII-gating logic; third-party data.
- **#SCHEMA-1 — pg_trgm indexes on core.users** · DB migration/schema change (new extension + indexes).
- **#SEC-6 — StaffMeController self-view nit** · touches authorization (even though trivial, keeping the "auth-touching → standalone" rule consistent; safe to fast-track alongside another standalone item at Josh's discretion).
