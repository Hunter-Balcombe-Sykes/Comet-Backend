# cross-repo bundle audit — CONSOLIDATED — 2026-07-23

## Scope

- **Lens:** bundle 'cross-repo' — frontend↔backend contract drift (XREPO-*) and cross-repo/frontend dead code (XDEAD-*). XREPO hunts routes/capabilities/flags wired on only one side of the boundary — most critically a frontend call to a backend route that no longer exists (a live 404, P1). XDEAD hunts frontend-side and cross-repo-provable dead code (backend intra-repo dead code stays with code-quality-slop). BOTH lenses require the adjudicator to grep BOTH repos before confirming ANY absence claim, and an UNRESOLVED entry from CONTRACT-INVENTORY.md may never become a finding without a source read in both repos — that asymmetry is the campaign's false-positive defense
- **Branch:** development
- **Generated by:** `scripts/audit/audit.sh` (DeepSeek scan + Claude adjudication)
- **Paths audited:**
    - `/Users/joshuahunter/Herd/Side Street/partna-frontend-main/app/(app)/account/(dashboard)/staff`

## Findings at a glance

| Tier | Count |
|------|-------|
| P0 — blockers | 0 |
| P1 — high     | 0 |
| P2 — medium   | 0 |
| P3 — low      | 2 |
| **Total**     | **2** |

## Execution policy  (how `execute audit` runs this file)

- **Plan:**       Opus 4.8
- **Implement:**  Sonnet 4.6
- **Review:**     Sonnet 4.6  — a *separate, independent* instance (never the implementer)
- **Combine plan+impl:** YES for S/XS effort · NO for P0/P1 or L/XL (those plan first, then implement)
- **Per-item override:** escalate to Opus for gnarly logic or risky blast radius; a trivial
  mechanical S item may drop to Haiku. Default to the table above unless an item clearly warrants a change.
- **Trigger:** say `execute audit <path to this file>` to run plan → implement → independent review
  per bundle/item. Blockers (P0 · auth · money · DB/migration · L/XL) pause for sign-off.
  Full runbook: `scripts/audit/fix-flow.md`.

---

# Cross-Repo Audit — 2026-07-23

**Branch:** development (backend) · `main` @ `5234954` (frontend, read-only ref)
**Lens:** Bundle 'cross-repo' — 2 focused themes: frontend↔backend contract drift (XREPO-*: routes/capabilities/flags wired on only one side of the boundary) and cross-repo/frontend dead code (XDEAD-*: frontend-side dead code provable only by reading both repos)
**Pipeline:** scan-tier draft by `deepseek-v4-pro`, adjudicated by `claude-opus-4.6`
**Source files audited:**
- audits/cross-repo/CONTRACT-INVENTORY.md
- partna-frontend-main/app/api/public/waitlist/route.ts
- partna-frontend-main/app/api/public/early-access/route.ts
- partna-frontend-main/app/(marketing)/waitlist/page.tsx
- partna-frontend-main/app/(marketing)/_components/early-access-form.tsx
- partna-frontend-main/app/(marketing)/_components/contact-form.tsx
- partna-frontend-main/app/api/public/booking/availability/route.ts
- partna-frontend-main/app/api/public/booking/checkout/route.ts
- partna-frontend-main/app/api/public/booking/config/route.ts
- partna-frontend-main/app/api/public/booking/services/route.ts
- partna-frontend-main/app/api/public/booking/_shared.ts
- supabase/migrations/20260719000000_drop_waitlist_signups.sql
- routes/api/platforms.php
- routes/api/user.php

## Progress

- P3 Low: 0 of 2 complete

---

## Note on the DeepSeek draft

Both scan-tier drafts for this run came back empty — the `frontend-backend-contract` lens draft is unexecuted preamble ("I'll start by reading the contract inventory...") with no findings, and the `cross-repo-dead-code` lens draft has no content at all. Per rule 5 ("Add findings DeepSeek missed"), the adjudicator worked the `CONTRACT-INVENTORY.md` reconciliation directly: every one of the 9 `FRONTEND-ONLY` candidates was grepped and read in both repos before any finding was accepted, and the 380-row `UNRESOLVED` bucket was spot-checked rather than exhaustively re-audited (per the campaign's own scope note, a route with no static caller isn't provable dead without exhausting dynamic call sites — that's a code-quality-slop concern, not this bundle's).

Of the 9 `FRONTEND-ONLY` rows, 5 were false positives dropped after verification:
- `api/public/*` — an unparseable wildcard artifact of the extraction script; no concrete call site exists to quote as Evidence.
- `api/public/contact` — only appears inside a code **comment** in `contact-form.tsx` ("Future: swap `onSubmit` to POST /api/public/contact once that endpoint exists"); the form actually submits via `mailto:` today. No live call exists.
- `api/public/pc`, `api/public/pv` — legitimate short aliases; both proxy cleanly to `POST /public/analytics/clicks` and (by the same pattern) `/public/analytics/pageviews`, which are in the `MATCHED` list. Not drift.

The remaining 4 rows collapse into the 2 findings below.

## P3 — Nice to have

- [ ] **#XDEAD-1** · P3 — Orphaned `/api/public/waitlist` BFF proxy targets a backend endpoint dropped 4 days ago
    - **Where:** partna-frontend-main/app/api/public/waitlist/route.ts:1-79
    - **Affects:** No real user — dead code only; a direct external POST to this path would surface a Laravel 404 forwarded as a 502/404 JSON body, not a crash.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Delete `app/api/public/waitlist/route.ts`.
        - Confirm (already verified) that `app/(marketing)/waitlist/page.tsx` renders `<EarlyAccessForm>` → `POST /api/public/early-access`, not this route.
        - Grep the frontend once more for `/api/public/waitlist` before merging to catch any last caller the adjudicator missed.
    - **Technical:** Backend commit-adjacent migration `supabase/migrations/20260719000000_drop_waitlist_signups.sql` dropped `core.waitlist_signups` outright and retired `PublicWaitlistController` — grepping `app/` confirms neither the controller nor the table reference exists anywhere in the backend anymore, only `EarlyAccessSignup`/`PublicEarlyAccessController`. The frontend's `waitlist/route.ts` still POSTs to `${apiBaseUrl}/public/waitlist`, a path with no backend match. Grepping the whole frontend repo for `/api/public/waitlist` and `/api/public/early-access` shows the `/waitlist` marketing page already migrated to `EarlyAccessForm` (posts to `/api/public/early-access`) — nothing in the UI still calls the old proxy. It's pure carry-over dead code from the backend-side retirement, not a live-user-facing 404.
    - **Plain English:** The website has a leftover "phone line" (`/api/public/waitlist`) that used to connect visitors signing up for the waitlist to a backend mailbox — but that mailbox was closed down a few days ago and replaced with a new one ("early access"). The actual sign-up page already dials the new number, so no visitor hits the old dead line today, but it's still sitting there wired up and should be unplugged so nobody stumbles onto it later.
    - **Evidence:**
        ```ts
        // POST /api/public/waitlist — forwards a waitlist signup to the Laravel backend.
        // The landing page ("/") posts an email here; the backend controller
        // (PublicWaitlistController::store) upserts into core.waitlist_signups keyed by email_lc.
        ```
        ```sql
        -- 20260719000000_drop_waitlist_signups.sql
        --
        -- Retire the V2 waitlist capture path. core.early_access_signups
        -- (20260711000300) superseded it with a full waitlist → invited → signed_up
        -- lifecycle plus staff CRUD; core.waitlist_signups had no read path at all
        ...
        DROP TABLE IF EXISTS core.waitlist_signups;
        ```

- [ ] **#XDEAD-2** · P3 — Whole public-booking marketplace BFF proxy layer (4 routes + shared helper) targets backend endpoints that don't exist post commerce strip-down
    - **Where:** partna-frontend-main/app/api/public/booking/{availability,checkout,config,services}/route.ts, partna-frontend-main/app/api/public/booking/_shared.ts
    - **Affects:** No real user — zero internal callers found anywhere in the frontend repo; dead infrastructure only.
    - **Effort:** M (~2h) — 5 files to remove plus a repo-wide grep pass to be sure nothing else imports `_shared.ts`.
    - **What to do:**
        - Delete `app/api/public/booking/availability/route.ts`, `.../checkout/route.ts`, `.../config/route.ts`, `.../services/route.ts`, and `.../booking/_shared.ts` together (only these 4 files import `_shared.ts`, confirmed by grep).
        - Do **not** attempt to restore the backend `/public/booking/*-by-slug` endpoints — the underlying commerce/booking-checkout capability was intentionally removed 2026-05-22 (Standalone Strip-Down); reintroducing it is a post-pilot platform decision, not this cleanup.
        - Leave the unrelated, still-live booking surfaces alone: `PATCH /api/booking/settings` (manual external-URL booking link) and `platforms/{provider}/booking/detect|status|forget` (`BookingController`) are current, backend-confirmed routes and are out of scope here.
    - **Technical:** Each of the 4 route handlers proxies to a `-by-slug` backend path (`/public/booking/availability-by-slug`, `-checkout-by-slug`, `-config-by-slug`, `-services-by-slug`) via the shared `buildBookingUpstreamTargets` helper. None of these four paths appear anywhere in the backend's 387-route surface (`routes/api/platforms.php` and `routes/api/user.php` show booking is now only `BookingController::detect/detectStatus/status/forget` under `platforms/{provider}/booking` plus the single `PATCH /booking/settings` manual-link field) — this is the marketplace-style customer checkout/availability flow that was removed in the 2026-05-22 commerce strip-down, and the frontend proxy layer was never cleaned up. A repo-wide grep for `/api/public/booking` turns up only the 4 route files themselves (no `.tsx` page or component fetches them), confirming these are dead, not reachable from any current UI flow.
    - **Plain English:** The website still has a whole set of "booking checkout" wiring plumbed in from before the business decided to strip out paid-booking/commerce features. Nothing in today's app actually uses this wiring — no button, no page — and even if something did, the backend side that used to answer these requests is gone. It's safe, unused scaffolding left over from an earlier version of the product and should be torn out so it doesn't confuse a future developer into thinking booking checkout still works.
    - **Evidence:**
        ```ts
        // POST /api/public/booking/checkout — completes a booking for a given sitepage slug.
        // Proxies to POST /public/booking/checkout-by-slug on the Laravel backend.
        ```
        ```php
        Route::prefix("{$base}/booking")
            ...
            Route::post('/detect', [BookingController::class, 'detect']);
            Route::get('/detect/status', [BookingController::class, 'detectStatus']);
            Route::get('/status', [BookingController::class, 'status']);
            Route::delete('/', [BookingController::class, 'forget']);
        ```

## Suggested Bundled Sessions

- **Bundle 1 — Delete orphaned public BFF proxy routes:** #XDEAD-1, #XDEAD-2
    - **Why grouped:** Same root-cause pattern — Next.js `app/api/public/*` proxy routes left behind after a backend-side feature retirement (waitlist→early-access on 2026-07-19; commerce/booking strip-down on 2026-05-22), with zero live callers in either case. Both are pure file deletions with a confirming grep, no shared code otherwise.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet (standard — trivial, low-risk deletions, no escalation needed).

## Standalone — do NOT bundle

None — neither finding touches auth/authorization, money, or a DB migration/schema change, and both are S/M-effort file deletions safe to ship together.
