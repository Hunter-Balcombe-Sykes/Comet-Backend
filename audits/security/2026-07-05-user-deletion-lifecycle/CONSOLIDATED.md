# user-deletion-lifecycle audit — CONSOLIDATED — 2026-07-05

## Scope

- **Lens:** lens-file: lifecycle-correctness
- **Branch:** development
- **Generated by:** `scripts/audit/audit.sh` (DeepSeek scan + Claude adjudication)
- **Paths audited:**
    - `app/Jobs/Account`
    - `app/Jobs/Gdpr`
    - `app/Services/User`
    - `app/Http/Controllers/Api/User`
    - `app/Http/Middleware/Context`
    - `routes/console.php`

## Findings at a glance

| Tier | Count |
|------|-------|
| P0 — blockers | 0 |
| P1 — high     | 0 |
| P2 — medium   | 6 |
| P3 — low      | 2 |
| **Total**     | **8** |

## Execution policy  (how `execute audit` runs this file)

- **Plan:**       Opus 4.8
- **Implement:**  Sonnet 4.6
- **Review:**     Sonnet 4.6  — a *separate, independent* instance (never the implementer)
- **Combine plan+impl:** YES for S/XS effort · NO for P0/P1 or L/XL (those plan first, then implement)
- **Per-item override:** escalate to Opus for gnarly logic or risky blast radius; a trivial
  mechanical S item may drop to Haiku. Default to the table above unless an item clearly warrants a change.
- **Trigger:** say `execute audit <path to this file>` to run plan → implement → independent review
  per bundle/item. Blockers (P0 · auth · money · DB/migration · L/XL) pause for sign-off.
  Full runbook: `scripts/audit/fix-flow.md`.

---

# Lifecycle Correctness Audit — 2026-07-05

**Branch:** development
**Lens:** Lifecycle correctness: race-safety, idempotency, anchor decoupling, reconcile loops, vendor resilience, observability discipline
**Pipeline:** scan-tier draft by `deepseek-v4-pro`, adjudicated by `claude-sonnet-4.5`
**Source files audited:**
- app/Jobs/Account/SendAccountDeletionRequestMailJob.php
- app/Jobs/Gdpr/ExportUserDataJob.php
- app/Services/User/AccountDeletionService.php
- app/Services/User/Concerns/ResolvesDeletedEmail.php
- app/Services/User/ConfirmationPreferenceService.php
- app/Services/User/DataExport/DataExportPayloadBuilder.php
- app/Services/User/DataExport/DataExportService.php
- app/Services/User/DataExport/DataExportZipWriter.php
- app/Services/User/SectionVisibilityService.php
- app/Services/User/SiteProvisioningService.php
- app/Services/User/UserBootstrapService.php
- app/Services/User/Visibility/Rules/*.php, SectionVisibilityContract.php, SectionVisibilityRegistry.php
- app/Http/Controllers/Api/User/Account/*.php (MfaController, SessionController, UserAccountDeletionController, UserDataExportController, UserDocumentController, UserSelfController)
- app/Http/Controllers/Api/User/Analytics/UserAnalyticsController.php
- app/Http/Controllers/Api/User/Customers/UserCustomerController.php, UserEnquiryController.php
- app/Http/Controllers/Api/User/Feedback/FeedbackController.php
- app/Http/Controllers/Api/User/Notifications/*.php
- app/Http/Controllers/Api/User/Site/HandleReclaimController.php, SubdomainAvailabilityController.php
- app/Http/Controllers/Api/User/SiteManagement/*.php
- app/Http/Controllers/Api/User/Uploads/*.php
- app/Http/Middleware/Context/EnforcePendingDeletionReadOnly.php, LoadCurrentUser.php
- routes/console.php
- supabase/migrations/20260526000000_baseline_standalone_user.sql
- supabase/migrations/20260612140000_site_custom_domain.sql

## Progress

- P0 Blockers: 0 of 0 complete
- P1 High: 0 of 0 complete
- P2 Medium: 6 of 6 complete
- P3 Low: 0 of 2 complete

---

## P2 — Should fix

- [x] **#LIFE-1** · P2 — Synchronous Cloudflare API calls in CustomDomainController block dashboard responses
    - **Resolution (2026-07-11):** Bounded all three CF calls (`create`/`get`/`delete`) with explicit HTTP timeouts (10s/10s/5s), mirroring `SupabaseAdminService`. This closes the acute risk the finding names (a hung vendor call pinning the PHP-FPM worker). Full async conversion is deliberately DEFERRED — it changes the `store()` response contract (`cloudflare => $created` carries the CF ownership/SSL DNS records the dashboard shows the user) and needs frontend coordination; the feature is gated off today (`configured()` false). Flagged for a coordinated cross-stack follow-up.
    - **Where:** app/Http/Controllers/Api/User/SiteManagement/CustomDomainController.php (`store`, `verify`, `destroy`)
    - **Affects:** Users configuring a custom domain — Cloudflare for SaaS API latency (typically 200–800ms, occasionally seconds under a control-plane blip) propagates directly into the request the dashboard is waiting on.
    - **Effort:** M (~2–4h)
    - **What to do:**
        - Dispatch the Cloudflare hostname create/verify/delete calls to a queued job instead of calling `$this->cf->create()` / `->get()` / `->delete()` synchronously inside the controller action.
        - Have the job update `custom_domain_status` / `custom_domain_cf_id` on completion; return 202 from the controller with the current (pending) state, same shape `state()` already returns.
        - Mirror the pattern already used for KV: `SyncSubdomainToKvJob::dispatch(...)` in this same file is async — the CF hostname calls should follow suit.
    - **Technical:** All three lifecycle actions (`store`, `verify`, `destroy`) make a live outbound HTTP call to Cloudflare while the PHP-FPM worker holds the request open. This is a one-off per-user action (not the sitepage-resolution hot path), but the class of bug is the same one this codebase has already fixed once for `SyncSubdomainToKvJob` — a vendor round-trip sitting inside the user-facing request cycle. Since the endpoint is gated by `$this->cf->configured()`, today's blast radius is limited to whenever custom domains go live; fix it before that flag flips.
    - **Plain English:** When someone connects their own domain name, the dashboard phones Cloudflare and waits for an answer before it can tell the user anything. If Cloudflare is slow that day, the user just stares at a spinner. The fix is the same one already used for the subdomain routing: leave a "we're on it" message and finish the job in the background.
    - **Evidence:**
        ```php
        try {
            $created = $this->cf->create($domain);
        } catch (Throwable $e) {
            report($e);

            return $this->error('Could not start domain verification with Cloudflare. Please try again.', 502);
        }
        ```
        ```php
        try {
            $status = $this->cf->get($site->custom_domain_cf_id);
        } catch (Throwable $e) {
            report($e);

            return $this->error('Could not read domain status from Cloudflare.', 502);
        }
        ```

- [x] **#LIFE-2** · P2 — Synchronous Supabase Admin API call in MfaController::destroy blocks dashboard response
    - **Resolution (2026-07-11):** Already satisfied — `SupabaseAdminService::unenrollMfaFactor()` has carried `Http::timeout(5)` since commit `4b50f615` (2026-05-18, when the endpoint was created). The finding's own recommendation was to keep it synchronous and "at minimum add an explicit timeout"; that timeout predates the audit. No code change needed.
    - **Where:** app/Http/Controllers/Api/User/Account/MfaController.php (`destroy`)
    - **Affects:** Users unenrolling an MFA factor — a slow Supabase Admin API response holds the HTTP request open for the full round-trip.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Either accept the current latency (MFA unenroll is a rare, user-initiated action with an already-tight 60s fresh-AAL2 window that argues for staying synchronous so the success/failure is immediately actionable), or queue the Supabase call and poll — but if queuing, the fresh-AAL2 window check must still happen synchronously before enqueue.
        - At minimum, add an explicit timeout to the underlying HTTP client used by `SupabaseAdminService` so a hung vendor call can't pin the worker indefinitely.
    - **Technical:** `$this->admin->unenrollMfaFactor($uid, $factorId)` is a live call to the Supabase Admin API made inside the controller action, after the fresh-AAL2 gate passes. Unlike the CF and Mail instances of this pattern, this one has a legitimate reason to stay synchronous (the caller needs to know immediately whether the factor was removed), so the fix here is bounding the vendor call's timeout rather than moving it off-cycle — flagging it so the timeout gets added deliberately rather than left to the HTTP client's default.
    - **Plain English:** Removing a two-factor login method calls out to Supabase and waits for confirmation before telling the user it worked. That's reasonable to keep synchronous, but there's currently no explicit "give up after N seconds" — so a slow Supabase response could leave the request hanging longer than it should.
    - **Evidence:**
        ```php
        try {
            $this->admin->unenrollMfaFactor($uid, $factorId);
        } catch (\RuntimeException $e) {
            Log::warning('MFA unenroll failed against Supabase Admin API', [
                'operation' => __METHOD__,
                'user_id' => $uid,
                'factor_id' => $factorId,
                'reason' => $e->getMessage(),
            ]);

            return $this->error('Could not remove factor', 502);
        }
        ```

- [x] **#LIFE-3** · P2 — Deletion-cancelled confirmation mail sent synchronously
    - **Where:** app/Services/User/AccountDeletionService.php (`sendDeletionCancelledMail`, called from `cancel`/`adminCancel`)
    - **Affects:** Users cancelling account deletion — SMTP connection/TLS handshake latency adds directly to the HTTP response time.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Replace `Mail::to(...)->send(...)` with `Mail::to(...)->queue(...)`, matching the pattern the sibling method `executeConfirmation()` already uses (`Mail::to($realEmail)->queue(...)`) two methods above it in the same class.
    - **Technical:** `sendDeletionCancelledMail()` runs outside any DB transaction (correct — it must not hold a lock across an SMTP round-trip) but still calls the synchronous `->send()` variant, which blocks the PHP-FPM worker for the full mail-driver round-trip. The same class's `executeConfirmation()` already established the queued pattern for the scheduled-deletion mail; this method is the one holdout.
    - **Plain English:** Clicking "cancel my deletion" sends a confirmation email, but the app waits for that email to actually leave the outbox before it tells the user it worked. If the mail server is slow, the user waits with it. The neighboring "schedule deletion" flow already fires its email in the background — this one just needs the same treatment.
    - **Evidence:**
        ```php
        private function sendDeletionCancelledMail(User $professional): void
        {
            try {
                Mail::to($professional->primary_email)->send(
                    new AccountDeletionCancelledMail(
                        displayName: (string) ($professional->display_name ?? 'there'),
                    )
                );
            } catch (\Throwable $e) {
                Log::error('Account deletion cancelled mail failed', [
                    'user_id' => $professional->id,
                    'error' => $e->getMessage(),
                ]);
            }
        }
        ```

- [x] **#LIFE-4** · P2 — Concurrent deletion confirmations with the same valid token both succeed
    - **Resolution (2026-07-11):** Implemented the fresher 2026-07-08 diagnosis of this bug (07-08 `#LIFE-3`, re-tiered **P1** — it also double-fires the scheduled-deletion mail, not just the audit row). Added an idempotency guard: a `lockForUpdate()` re-read as the first statement inside `executeConfirmation()`'s transaction, no-op when `status === 'pending_deletion'` already. Keyed on **status, not `deletion_token_hash`** (as the 07-08 finding literally suggested) — `executeConfirmation()` is shared with the tokenless `adminInitiate()` path, so a token-based guard would have wrongly bailed on every admin-initiated deletion. Loser-of-race returns before the post-transaction mail/cache side effects. Verified by revert-and-rerun (duplicate mail fires without the guard) + `AdminInitiatedDeletionTest` green (admin path intact).
    - **Where:** app/Services/User/AccountDeletionService.php (`confirm`, `executeConfirmation`)
    - **Affects:** Users confirming account deletion — a double-click or double-tab race sends duplicate scheduled-deletion emails and writes duplicate audit rows.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add a `lockForUpdate()` re-read of the User row at the top of `executeConfirmation()`'s transaction and re-check `deletion_token_hash` against the hash the caller validated, bailing (no-op) if it no longer matches — the same lock-then-recheck shape `DataExportService::dispatch()` already uses for its in-flight-export dedup check, and the same tokenHash-recheck shape `SendAccountDeletionRequestMailJob::handle()` already uses.
        - Alternative: a single conditional `UPDATE ... WHERE deletion_token_hash = ?` so the loser sees zero affected rows.
    - **Technical:** `confirm()` loads `$professional` once per request (no row lock) and checks `hash_equals($professional->deletion_token_hash, ...)` against that in-memory snapshot. Two concurrent requests carrying the same valid token both pass this check before either has written anything, then both call `executeConfirmation()`, which unconditionally updates status, sends `AccountDeletionScheduledMail`, and creates a `UserDeletionAuditEntry` — with no re-verification that the token is still the one on file. Contrast with the codebase's own `request()` flow, which is protected downstream: `SendAccountDeletionRequestMailJob::handle()` re-locks the row and re-checks the token hash before sending, so a duplicate `request()` race silently loses. `confirm()`/`executeConfirmation()` has no equivalent downstream guard.
    - **Plain English:** If a user opens the "confirm deletion" link in two tabs at once, both can succeed. They get two "your account will be deleted" emails and the audit trail logs the confirmation twice. It's like two clerks each stamping "APPROVED" on the same form because neither checked whether the other already had.
    - **Evidence:**
        ```php
        if (! hash_equals((string) $professional->deletion_token_hash, hash('sha256', $rawToken))) {
            return ['success' => false, 'code' => 404, 'error' => 'Invalid token.'];
        }

        $deletesAt = $this->executeConfirmation(
            $professional,
            UserDeletionAuditEntry::EVENT_CONFIRMED,
            $request,
        );
        ```
        ```php
        $professional->update([
            'deletion_previous_status' => $previousStatus,
            'status' => 'pending_deletion',
            'deletion_confirmed_at' => now(),
            'deletion_token_hash' => null,
        ]);
        ```

- [x] **#LIFE-5** · P2 — Custom domain claim check is TOCTOU-racy with no UniqueConstraintViolationException handling
    - **Where:** app/Http/Controllers/Api/User/SiteManagement/CustomDomainController.php (`store`)
    - **Affects:** Two sites racing to claim the exact same custom domain at the same moment — the loser gets an uncaught 500 and a Cloudflare custom-hostname resource is created and never cleaned up.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Wrap the `$site->save()` call in a `try { ... } catch (\Illuminate\Database\UniqueConstraintViolationException $e)` and, on catch, call `$this->cf->delete($created['id'])` to remove the just-created (now-orphaned) Cloudflare hostname, then return the existing "That domain is already connected to another Partna site." 422.
        - This closes the gap between the app-level `exists()` pre-check and the DB-level guarantee.
    - **Technical:** `site.sites` has `CREATE UNIQUE INDEX CONCURRENTLY sites_custom_domain_unique ON site.sites (lower(custom_domain)) WHERE custom_domain IS NOT NULL` (migration `20260612140000_site_custom_domain.sql`), so the database *does* enforce this correctly — but the controller's `$takenByOther` pre-check is a plain `exists()` query with no lock, and nothing in `store()` catches a `UniqueConstraintViolationException` around the later `$site->save()`. Two requests racing to claim the identical domain string both pass the pre-check, both call `$this->cf->create($domain)` (creating two Cloudflare custom hostnames for one domain — a vendor-side resource neither request will ever clean up), and the losing `$site->save()` throws an uncaught exception that surfaces as a raw 500 instead of the friendly 422 the pre-check path returns for the exact same logical condition.
    - **Plain English:** The app checks "is this domain already taken?" before saving, but two people trying to claim the identical domain at the same instant can both pass that check. One of them then hits a generic server error instead of a clear "already taken" message, and Cloudflare is left holding a domain-verification record nobody will ever finish or remove.
    - **Evidence:**
        ```php
        // Reject a domain already connected to a different site.
        $takenByOther = Site::query()
            ->whereRaw('lower(custom_domain) = ?', [$domain])
            ->where('id', '!=', $site->id)
            ->exists();
        if ($takenByOther) {
            return $this->error('That domain is already connected to another Partna site.', 422);
        }
        ```
        ```php
        $site->custom_domain = $domain;
        $site->custom_domain_cf_id = $created['id'] ?? null;
        $site->custom_domain_status = $active ? 'active' : 'pending';
        $site->custom_domain_verified_at = $active ? now() : null;
        $site->custom_domain_primary = $active;
        $site->save();
        ```
        ```sql
        CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS sites_custom_domain_unique
            ON site.sites (lower(custom_domain)) WHERE custom_domain IS NOT NULL;
        ```

- [x] **#LIFE-6** · P2 — Email-uniqueness check in UserBootstrapService is TOCTOU-racy
    - **Resolution (2026-07-11):** Two-part fix in `UserBootstrapService::bootstrap()`. (1) Typed catch around `$professional->save()` maps an email-index `UniqueConstraintViolationException` (the DB backstop for the racy pre-check) to the same `EMAIL_ALREADY_REGISTERED` the pre-check throws → HTTP 409 instead of a raw 500; discriminates on the index name in the driver message (a re-query is impossible after a violation inside a Postgres transaction) so a `users_auth_user_id_unique` violation re-throws unchanged. (2) Migration `20260711170000_users_email_unique_case_insensitive.sql` re-creates the unique index on `lower(primary_email)` (CONCURRENTLY build-swap-rename per CONVENTIONS §1) so the DB enforces exactly what the app checks. Dev dup-check ran clean (0 case-insensitive dups) before writing. **Migration committed but NOT yet applied.** **Also resolves the fresher 2026-07-08 `#LIFE-12`** (last-write-wins on concurrent existing-user updates): `$existing` is now re-fetched under `lockForUpdate()` inside the transaction (the pre-transaction read stays only for the fast disabled-account 403). Verified: `composer guard:no-unsafe-migrations` passes; catch revert-proved; 84 bootstrap tests green.
    - **Where:** app/Services/User/UserBootstrapService.php (`guardAgainstEmailReuseByDifferentAuthUser`, called inside `bootstrap()`'s transaction)
    - **Affects:** New-user signup — concurrent bootstraps with the same email can both pass the pre-check; the loser hits an unhandled DB unique-violation → 500 instead of the intended `EMAIL_ALREADY_REGISTERED` error.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Catch `\Illuminate\Database\UniqueConstraintViolationException` around `$professional->save()` in `bootstrap()` and re-throw as the same `RuntimeException('EMAIL_ALREADY_REGISTERED')` the pre-check throws, so both paths give the caller one consistent error to handle.
        - Separately (standalone — schema change): `core.users.primary_email`'s unique index is `CREATE UNIQUE INDEX users_email_unique ON core.users (primary_email) WHERE (deleted_at IS NULL)` — case-sensitive — while the application's own duplicate check compares `lower(primary_email)`. Two signups whose emails differ only by case (e.g. `Foo@Bar.com` vs `foo@bar.com`) can both insert successfully today; the DB constraint doesn't catch what the app logic assumes it's preventing. Migrate the index to `lower(primary_email)` to close this gap.
    - **Technical:** `guardAgainstEmailReuseByDifferentAuthUser()` runs a case-insensitive `exists()` check inside the pgsql transaction, then `$professional->save()` follows with no typed catch around it. `core.users` does carry `users_email_unique` (verified in `supabase/migrations/20260526000000_baseline_standalone_user.sql`), so an identical-case concurrent duplicate will throw `UniqueConstraintViolationException` uncaught, propagating as a bare 500 on the signup path — the most-trafficked funnel entry point in the app.
    - **Plain English:** When two people try to sign up with the same email at nearly the same moment, the "is this email taken?" check can tell both of them "no" before either has actually registered. The second one then crashes with a generic server error instead of a friendly "that email is already registered" message — right at the front door of the product.
    - **Evidence:**
        ```php
        $existingByEmail = User::query()
            ->whereRaw('lower(primary_email) = ?', [$emailLc])
            ->where('auth_user_id', '!=', $uid)
            ->exists();

        if ($existingByEmail) {
            throw new RuntimeException('EMAIL_ALREADY_REGISTERED');
        }
        ```
        ```sql
        CREATE UNIQUE INDEX users_email_unique ON core.users (primary_email) WHERE (deleted_at IS NULL);
        ```

## P3 — Nice to have

- [ ] **#LIFE-7** · P3 — Welcome notification firstOrCreate lacks DB-level unique constraint
    - **Where:** app/Services/User/UserBootstrapService.php (`createWelcomeNotification`)
    - **Affects:** New users during signup — a rare concurrent double-bootstrap could create two "Welcome to Partna" notifications.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add a `UNIQUE (user_id, type, title)` index on `notifications.notifications` in a schema migration, then replace `firstOrCreate` with an upsert (`insertOrIgnore` or `ON CONFLICT DO NOTHING`).
    - **Technical:** The comment on this call already documents the gap explicitly: no DB-level constraint backs the dedup, so `firstOrCreate`'s SELECT-then-INSERT is not atomic. This is the same tracked, deliberately-deferred item referenced in the code as DINT-12 (deferred to the schema standalone, S8) — surfacing it here for completeness, not as new information. Given the acknowledged low frequency (same-user concurrent bootstrap) and existing tracking, this stays low priority.
    - **Plain English:** When a new user signs up, the app posts one "Welcome" notification — checking first, then creating. A very unlucky double-signup could create it twice. The code already flags this as a known, accepted shortcut, so this is a reminder, not a surprise.
    - **Evidence:**
        ```php
        // firstOrCreate dedups the welcome notification at the app level — there is NO DB unique constraint on (user_id, type, title), so a rare concurrent double-bootstrap could still race. Acceptable for a one-time bootstrap; the DB-level constraint is deferred to the schema standalone (S8). (DINT-12)
        Notification::query()->firstOrCreate(
            [
                'user_id' => $professional->id,
                'type' => 'Info',
                'title' => 'Welcome to Partna',
            ],
            [
                'body' => 'Your account is ready. Complete your profile and start building your professional page from your dashboard.',
                'cta_url' => null,
                'severity' => 'info',
                'starts_at' => now(),
                'ends_at' => null,
            ]
        );
        ```

- [ ] **#LIFE-8** · P3 — UserAccountDeletionController request/cancel lack explicit authorizeForUser gate
    - **Where:** app/Http/Controllers/Api/User/Account/UserAccountDeletionController.php (`request`, `cancel`)
    - **Affects:** Self-service deletion flows — no functional gap today (the `professional` actor is structurally the authenticated user, not a request parameter), but deviates from the Authorization Doctrine that every mutating action gate through a Policy.
    - **Effort:** S (~0.5–1h)
    - **What to do:**
        - Add `$this->authorizeForUser($professional, 'update', $professional)` to both `request()` and `cancel()`, matching the gate already present in `confirm()` two methods below.
    - **Technical:** `confirm()` calls `$this->authorizeForUser($professional, 'update', $professional)`; `request()` and `cancel()` do not, relying only on `EnforcePendingDeletionReadOnly` (which the `cancel` route explicitly exempts itself from, correctly, since cancelling requires a write) plus inline status checks. There is no exploit today: `$professional` in all three methods comes from `$request->attributes->get('professional')`, which is resolved server-side from the verified JWT, not from any client-supplied ID — so there is no path for a user to reach another account's row through these endpoints. This is a consistency/future-proofing gap: if a staff-facing route is ever routed through these same methods, the missing gate becomes a real one.
    - **Plain English:** Two of the three account-deletion endpoints skip the standard security checkpoint the third one uses. They're safe today because the front door is already locked structurally, but the house rule is that every room gets its own lock too, so the codebase stays predictable as it grows.
    - **Evidence:**
        ```php
        /** @var User $professional */
        $professional = $request->attributes->get('professional');
        // 'update' policy gates ownership + pending-deletion block; no separate 'confirm' ability needed.
        $this->authorizeForUser($professional, 'update', $professional);
        ```
        ```php
        public function request(Request $request): JsonResponse
        {
            /** @var User $professional */
            $professional = $request->attributes->get('professional');

            if ($professional->status === 'pending_deletion') {
        ```
        ```php
        public function cancel(Request $request): JsonResponse
        {
            /** @var User $professional */
            $professional = $request->attributes->get('professional');

            if ($professional->status !== 'pending_deletion') {
                return $this->error('No pending deletion to cancel.', 409);
            }
        ```

## Suggested Bundled Sessions

- **Bundle 1 — Move synchronous vendor calls off the request cycle:** #LIFE-1, #LIFE-2, #LIFE-3
    - **Why grouped:** same root-cause pattern (vendor round-trip blocking the HTTP response) across three different controllers/services; no shared file dependency, safe to fix together in one session.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

- **Bundle 2 — Race-safety hardening for account-deletion confirm + custom-domain save:** #LIFE-4, #LIFE-5
    - **Why grouped:** both are the same "check-then-act without a lock or typed exception catch" pattern; both fixes are self-contained code changes with no schema impact.
    - **Model:** Plan: Opus · Implement: Sonnet · Review: Sonnet.

## Standalone — do NOT bundle

- **#LIFE-6 — Email-uniqueness check in UserBootstrapService is TOCTOU-racy** · touches a schema change (case-insensitive unique index migration) and sits on the signup path; plan + sign-off before implementing.
- **#LIFE-7 — Welcome notification firstOrCreate lacks DB-level unique constraint** · requires a schema migration (new UNIQUE index); run alone even though effort is small.
- **#LIFE-8 — UserAccountDeletionController request/cancel lack explicit authorizeForUser gate** · touches authorization; per policy, anything touching auth always runs standalone regardless of tier or effort.
