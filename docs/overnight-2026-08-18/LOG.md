# Overnight run 2026-08-18 — LOG

Plan + rulings: `docs/2026-08-18-overnight-run-plan.md`. Times AEST.
Entry types: `F<n>` finding (probe evidence → fix → re-probe → gate) · `X<n>` unrelated bug fixed on the way · `BLOCKED-F<n>`.

## W0 — setup (02:45–03:20)

- Test account: `broken-oven` (auth `ff2e60cf-…`, core `019f936e-115f-…`, business/bar). Snapshot before wipe: 42 connections (1 live), 17 ingest.sources, 235 content.items, 199 media_assets, 100 pins, 1 workplace, 22 collections. **Wiped to 0 across the board** with new `partna:reset-test-user broken-oven --yes` (03:05). Retired-surface live rows across dev: **0** (guardrail query). Residue elsewhere: not touched.
- Local topology up: `artisan serve :8000` (8 CLI workers), 2 × `queue:work` (all queues), `schedule:work`; local Redis; dev Supabase via pooler; dashboard :3000 → `http://localhost:8000` (`.env.local` backed up). Browser session authenticated (`/api/me` 200 via :8000).
- `.env` backed up (`.env.backup-2026-08-18-pre-overnight`); local-override tail appended (APP_ENV=local, local Redis, MAIL log, PARTNA_MEDIA_DISK=public_dev to match dev).
- New dev-only commands: `partna:reset-test-user`, `ingest:run --source|--user [--key] [--sync]`, `partna:as <handle> <METHOD> <uri> [--json]` (real HTTP kernel, JWT stub as in tests). Verified `partna:as broken-oven GET /api/me` → 200.
- Baselines: dashboard typecheck ✅ lint ✅ (1 warning). Backend `composer test` running (see W0 close).
- MCPs live: Nightwatch (app a1698025), Supabase (dev ref, prod present → always pass dev ref), Laravel Cloud tinker (`cloud tinker development --code=`), supabase CLI linked to dev; migrations in sync through 20260819000300.
- Apify: US$3.91 / 29 used this cycle.

## W1 — connect matrix

Sweep method: `partna:connect-sweep broken-oven --fixtures=fixtures.json` (257 cases across 94 keys, url+handle forms; GB/IG by hand). Reports: scratchpad `sweep1.json` (business/bar), `sweep2-partna.json` (booking set as partna).

- **F1 (fixed, 0115202e0)** — watch pool auto-selected NOTHING after a YouTube ingest. Root cause: `SectionCandidates::latest_per_auto_source` tie-break `whereRaw('A > B OR (A = B AND id > id)')` unparenthesised → OR escaped same-source/kind filters; any equal-timestamp row in the table (another dev user's copy of the same video) made every candidate lose. Regression test `PoolLaneTest` (red without fix). Live re-probe: selection = 1 auto item, `latestItemId` set.
- **F3 (fixed, 0115202e0)** — `YoutubeRssConnector::resolveHandle` took the FIRST `"channelId"` on the channel page (a featured/related channel); @mkbhd resolved to `UCG7J20LhUeLl6y_Emi7OJrA`, wrong uploads landed. Now externalId → RSS link → /channel/ → channelId (same as YoutubeScraper). Regression test. Live: cursor `UCBJycsmduvYEL83R_U4JriQ`, newest = 2026-08-12 Pixel video auto-selected. NOTE for W9: existing youtube streams on dev may hold a wrong cached `channel_id` in `ingest.streams.cursor` — clear on deploy (`UPDATE ingest.streams SET cursor='{}' WHERE stream_name='watch' AND source_id IN (youtube sources)`).
- **X1** — orphan `content.source_items` (item_id NULL, from a hard-delete) get a blank item minted on the next projection. Only reachable via hard deletes (my reset). Reset command now removes `content.sources`; production path untouched.
- **F4 (fixed, 5b798742a)** — 43 brand-link connects sat `last_refresh_status=pending` forever with brand label as name; `connectBrand` never dispatched `EnrichLinkCardJob`. Now dispatched (og:title/description/favicon/logo → ok → bell). Live: github card → "torvalds - Overview", avatar logo, description; Patreon bot-blocks the scraper (name stays label → W7 handle extractor).
- **F7 (data fix, dev)** — skool + strava returned 503 "integration currently unavailable": stale July `core.feature_availability` global `disabled` rules from the retirement era; both are valid link-only surfaces now. Deleted the two rows. Live: both connect 200. (No code.)
- **F9 (fixed, 34be5deae)** — NowBookit `parseIds` required numeric `accountid`; every current booking link carries a UUID (both real fixtures). Accept UUID|digits. Test + live 200.
- **F10 (fixed, 34be5deae)** — Medium normalizer + catalog detector rejected dotted handles (`@julie.zhuo` is live). Bare dotted tokens read from raw input (urlish() promotes 'a.b' to a host). Test + live 200; catalog recompiled + synced.
- **F12 (fixed, this commit)** — handle inputs rejected on handle-capable brand platforms. Added `canonicalUrl` templates + `(?<handle>…)` capture detectors (with reserved-path rejects) to github, gitlab, ko-fi, patreon, buymeacoffee, codepen, dribbble, behance, substack (subdomain), tock; `BrandLinkConnect::normaliseInput()` expands a bare token through the surface template and `PlatformConnectRequest` merges the expanded URL before the host assertion. Live: `@torvalds`, `astralcodexten`, `astralcodexten.substack.com`, `hbomberguy`, `pixelmuse`, `the-french-laundry` all 200 with canonical URLs; menulog (no template) still 422 on a bare token. Test.
- Verified OK live (no fix needed): youtube (url+handle), youtube-music, bandcamp (kingkrule), vimeo (real user), soundcloud (url+handle), spotify (deferred 202→ready), apple music/podcast (url + free text), eventbrite/humanitix (deferred → ready, organiser payload), all 14 socials url+handle, opentable (rid), resdiary, 40+ brand cards, all 22 booking brands as partna, fresha (deferred → team mode, real Melbourne salons: team + services), instagram (real creator gypsea_lust → 12 media items via eager ingest, bio links captured), routing preview (place/note/reject).
- Fixture artefacts (not bugs): vimeo.com/staffpicks (channel path) + /nasa (404 upstream); Flume Spotify id in fixture was wrong (real `6nxWCVXbOlEVRexSbLsTer` oEmbeds 200); doyoutravel is a stub IG account (0 posts); circle/ordermate fixtures were apex/marketing pages.
- Observations carried to later workstreams: uber_eats/doordash brand connects create paid ingest sources that never run (W3/W9); Instagram bio links → 0 syncFindings on first pass (check in W3 router); IG reels land as media without f_playable (W4); linktr.ee pasted → router `reject: shortener` (W3 unroll); Google Business connect deferred to W3; dashboard connect-sheet items (`?platform=` open, deferred-45s-as-success, PLATFORM_POOL map missing spotify/soundcloud) → W7.
- Test env: `.env.testing` created from `.env.example` (gitignored) — the injected dev `.env` leaked `PARTNA_CONNECT_DEFERRED`/media disk/cache TTL into the suite and produced 4 false failures. With it: all previously-failing files green.

## W2 — pool rules + auto-sync toggles (03:25–04:00)

- **R5 media opt-in (5c1da8c25)** — new rule op `latest_n_per_auto_source` (N = `partna.pools.auto_latest_n`, default 5) honours `auto_sync_latest` AND google-business `photos`; media shape = kind_is + latest_n; `content:reshape-pool-sections {pool}` (generic; replaces media-only) rewrote 17 media + 12 stale shop sections on dev. Live: media pool selection = 5 newest of 12 IG items, all `origin=auto`. Tests: N window + photos-off suppression, shop reshape.
- **F13 (fixed)** — `recency` ordered by `last_seen_at` (projector touch time) → arbitrary order after a bulk ingest. Now published date desc, first_seen fallback, id tiebreak. Test.
- **Disconnect = hide (LiveSourceScope)** — items whose only sources are removed/inactive connections leave auto half, pins and library; rows kept. Live: disconnect youtube → watch selection 3→1, library 50→20, 50 items retained. Test (incl. reconnect brings back).
- **Toggles** — spotify + soundcloud now declare `auto_sync_latest` ("Latest track"); `GET /platforms/spotify/display-settings` 200. `DisplaySettingsController::show` merges all live accounts (any-on) instead of `first()`. Test.
- **Dashboard** — `SourceStack` shows `Latest` + `Auto` chips on selected rows (PoolTable stamps `isLatest` from `latestItemId`). Screenshot: watch row "Vineyard At Sunset" · vimeo · Latest · Auto.
- **Review (sonnet, fresh eyes) on W1** → 2 bugs fixed: `ingest:run` had no production guard; `BrandLinkConnect` treated any host as a Substack handle (`torvalds.github.io` → `*.substack.com`). Also corrected a wrong comment (merge() in after() never reaches validated(); resolve() re-normalises). Tests added.
- **F14 (open → W4)** — IG projection dispatched `MirrorMediaAssetJob` for only 2 of 88 assets; a hand dispatch mirrored 76/88 in 30s (R2 write OK, 1080px webp). Wire still shows CDN URLs for the unmirrored covers → broken images in the media grid. Root cause TBD in W4.
- **X2 (dashboard perf, open)** — Platforms page fans out one `/api/platforms/{key}/accounts` request per platform (~100); saturates a small server. Should be one call.
- **X3 (open)** — `routing.observation.write_failed`: `link_observations_source_check` violated during IG bio-link routing (17:35:31). Investigate in W3.
- Local: `.env` MEDIA_DISK_URL pointed at dev R2 URL (was a placeholder domain); laravel.log (8.3 GB) truncated.

## W3 — Google Business chain (04:00–04:40)

Real places: **Top Choice Restaurant** (Wollongong, website = linktr.ee), **Lower East by RÜH** (Wollongong, website ruhcafe.com.au). Connected via `POST /platforms/google-business/connect {placeId,name,address,lat,lng}` on a clean account.

- **Chain proven live (one connect → everything below, no hand steps):** Place Details card · Apify enrich (harvest keys tableReservationLinks/bookingLinks/instagrams/facebooks) · auto-connected `uber_eats.order`, `menulog.order`, `doordash.order` (Top Choice) / `uber_eats.order`, `bopple.order`, `nowbookit.reserve` (RÜH) · `facebook.profile` + `instagram.profile` (real handles → InstagramConnectJob → eager IG ingest → media) · workplace seeded (name/phone/website) · previous-website scan (about/menu/accent/logo) · `ResolveSiteAccentJob` · `BuildSiteDocumentJob` · MenuFetchJob (Uber Eats actor → 33 dishes / 7 collections for Top Choice; RÜH 14+ dishes) · GB eager ingest → 5 reviews + 10 Google photos + source_stats 4.1★/479 · CommerceProbe on shop-shaped links · EnrichPoolLink for custom links · media mirrors · edge purges.
- **F15 (fixed)** — a link-in-bio "website" (Top Choice's linktree) was harvested (0 anchors) and pasted linktrees were `reject: shortener`. Now unrolled via `LinkInBioScanJob` from GB enrich, previous-website scan and the router (paste → note + outcome `link`, preview → note). Live: 114 links seen → 3 seeded + 1 pending; RÜH's IG-bio linktree → 6 custom links + 3 seeded (bopple ×3 venues, nowbookit, mryum, fundraiser…).
- **F16 (fixed, R8)** — GB/spotify/soundcloud sources never ran. `eagerOnConnect: true` on those three connectors; `SourceProvisioner::schedulable()` allow-lists `partna.ingest_scheduled_paid_sources` (default gb,spotify,soundcloud). Live: GB eager run 48s → reviews/media landed. Tests.
- **F17 (fixed)** — an ordering connection written by the router/linktree (RÜH's `uber_eats.order`) never triggered `MenuFetchJob` (only `GoogleBusinessAutoSync`'s own seed did). Observer now dispatches it for any ordering row on a menu-platform host. Test. Live: RÜH menu 0 → 14+ dishes after a re-scan.
- **R14 (done)** — partna accounts get workplace + socials + booking from a GB connect; reservations/ordering stay capability-gated. Test updated.
- **R13 logo → vector (verified live)** — processor `/health` 200; `website_scan.logo_grab` on ruhcafe.com.au: `logo_full: inline-svg → uploaded (svg)`, `logo_square: … → uploaded (svg)`. Note: `grabIfEmpty()` skips when the account already has logos (my reset keeps design media — cleared by hand for this proof).
- **X3 (fixed, migration 20260819001000 applied to dev)** — `routing.link_observations.source` CHECK lacked `commerce_probe` (source_intents had it since 20260814); every CommerceProbe observation write failed silently.
- **Ops lesson** — `pkill` of queue workers left `ShouldBeUnique` locks (GB enrich, MenuFetch, GoogleMenuPhotoScan, 900–1800s) dangling and silently dropped the next connect's jobs. Restart script now uses `queue:restart` (graceful). Stale locks cleared by hand.
- Full suite after W1: 8313 passed, 3 failed — all three the routing-corpus guards tripped by F12's new detectors; corpus regenerated + floor test updated (profile-shaped URL on a handle brand = ProfileLink, host-only shapes stay below suggest).
- Open → W4: F14 (IG projection dispatches too few mirrors); GB photo identity/selectable (R6). Open → W9: menus ingest sources (uber_eats/doordash/square) idle beside the live MenuFetchJob lane — decide/retire; MenuFetchJob at 18:25:42 ran BEFORE the ordering seed (plan null) — F17 makes that harmless.

## W4 — media pool (04:45–05:20)

- **F14 (fixed, 96ecea22f)** — `ProjectionWriter::resolveMediaAssets` returned before `dispatchMirrors()` when no asset was newly minted; the legacy Instagram seeder mints the same fingerprints first, so 86/88 IG frames were never mirrored. Mirror pass now always runs (its `needing` query filters `storage_path IS NULL`). Test (red without fix). Live: replay dispatched 13 mirrors for the unmirrored assets.
- **R6 (done, c6031ca0b)** — Places `photos[].name` rotates on every Details call (probed: 0/10 overlap seconds apart) but `googleMapsUri` and the `postId` in `flagContentUri` are constant (10/10). GB photo records now keyed `gphoto:<postId>` (→ maps-uri hash → name fallback). Live: two fresh fetches → item count constant (no new items), URLs refreshed. `BorrowedMedia::BORROWED_SOURCE_KEYS` emptied → GB photos pinnable (D5 lifted; seam kept). Tests. Dev cleanup: 50 legacy name-keyed GB items + source_items retired, record_state tombstoned. **Prod deploy step: run the same retire SQL after deploy.**
- **R7 (done, c6031ca0b + monorepo 874276e)** — reel = cover still + `video` frame (mp4, instagram-owned ref → `MediaMirror` stores raw .mp4 to R2 under content hash, 80 MB cap via `SafeUrlFetcher::withMaxBytes`); `PoolResolver::frames()` emits `kind` + `poster`; migration 20260819001100 admits `item_media.role='video'` (applied to dev). Astro: `resolve-site-content` hero picks the video frame, `MediaCard` renders `<video autoplay muted loop playsinline poster>`; dashboard media tile shows a play chip. Live: gypsea_lust connect → 2 reels, both mirrored (`…/5ee5e4b9…mp4`, `…/b98eb007…mp4`), wire frame `('video','video','https://fls-…laravel.cloud/…mp4', poster=cover)`. Tests: projector, mirror (ftyp), wire frames.
- Auto-fill N on connect = the media rule (`latest_n_per_auto_source`, N=5) — nothing else needed; IG `auto_sync_latest` absent = ON at connect.
- Observation: the two reel mirror jobs dispatched at connect did not land before I queued them by hand (a queued job then mirrored in 2s). Not reproduced; watch on next IG connect (W9 resync proof).
- W3 review fixes landed in 96ecea22f (link-in-bio branch mirrors the note branch's cap/busy/unavailable handling and dispatches after the write; scan job hunk that was missing from bfcb087b9; enrich skips harvest for a link-in-bio site; Square `^order.` keeps Square Online domains but excludes other brands' `order.` hosts).
- Local: Places per-user cap (60) hit on the test account (budget guard proven); raised locally to 400 for the run; effect freshness 300s locally.

## W5 — identity, links, images (05:20–06:05)

- **R10 Apple songs (5dc7868df)** — `AppleMusicConnector` gets a `songs` stream (entity=song → `track` via `AppleMusicTrackProjector`), artwork upscaled to 1200×1200 (mzstatic path rewrite; releases too). Runtime pulls per stream, so `pull()` switches on `$pull->stream->name` (first cut mixed streams → `shape` on both; fixed). Live: Tame Impala → 200 songs + 44 albums, tracks 25→204.
- **Primary-artist TitleRelease** — `KeyClass::primaryArtist()` (first credited artist) feeds TitleRelease so "Tame Impala, JENNIE" meets "Tame Impala". Test.
- **Edition-aware poisoning** — a same-source title_release duplicate whose durations agree (±2s) is two editions of one song, not unreliable data; stays live. Live: Spotify↔Apple unions 3 → **9 of 10** top tracks (Dracula, Dracula (JENNIE Remix) ↔ "Dracula - JENNIE Remix", Borderline, Eventually, Is It True, Loser, …). Wire: one listen item with `spotify` + `apple-music` synced links and Apple's 1200px cover. Test (edition dupes union; intro-vs-song still poisons).
- **Menu per-platform links** — `ItemLinkRules::ROSTER['menus']` + hosts + `platformForUrl()`; `PoolResolver` derives a synced link per offer store url. Live: 42/42 RÜH dishes carry `uber-eats` links.
- **Best cover** — `PoolResolver::cover()` picks the largest measured area within a role, source priority as tie-break.
- Full-suite fallout (6d8999213): `LiveSourceScope` no longer requires a non-retired source_item (source absence is folding's job) — DocumentBuilderRuleOps 'Retired' semantics restored; DocumentBuilder tests get the full content schema; rule-ops fixture inserts a live connection row; observer reads `CardPayload::url()` (NoUntypedPayloadAccessTest); OnlineOrdering MAX_ENTRIES test seeds before Queue::fake.
- Deferred: mirroring non-Instagram art (Apple mzstatic is stable; Uber Eats CDN signed) — MediaMirror OWNED_REF_PREFIXES stays `instagram:` (cost/terms; note for morning). 1 of 42 RÜH dishes has no name from the scrape (X4, cosmetic).

## W9 — resync (06:05–06:35) + W7 backend naming

- **Resync → ingest (fdabfc184)** — `POST /platforms/{p}/refresh` now also claims + dispatches `RunSourceJob` for the connection's ingest sources (paid too, budgets gate). Live: youtube Resync → `ingest.runs` row (`schedule`, ok, 15 records) within 45s; second click while in flight dispatches nothing (429 cooldown / claim). Test.
- **Cadence proven** — bumped another user's youtube source `next_attempt_at=now`; the 15-min `ingest:dispatch` tick at 20:15:10 UTC ran it (`schedule`, ok). `ingest:backfill-sources` flipped auto_sync ON for allow-listed paid sources on dev (google_business 12/14, spotify 3/4, soundcloud 2/2 — remaining rows are trashed connections). Note: EWMA backoff pushes an unchanged youtube source ~3 days out; acceptable, revisit min interval if owners complain.
- **Legacy pending sweep** — `platforms:enrich-pending-cards` (dev: 33 → 1 still pending), scheduled daily 03:20. Unknown-connector `ingest.sources` (twitch ×2, substack ×1) deleted on dev.
- **Double-fetch (legacy RefreshConnectionJob for youtube/vimeo/music) — left in place**: the payload still feeds `/platforms/{p}/accounts` (name/handle/avatar); retiring it needs the accounts endpoint moved onto content.* first. Recorded, not done.
- **W7 backend (R9)** — `ConnectionDisplayName::for(surface, payload)`: display_name → the connected thing's name (never the brand label, "| Brand"/"- Overview" tails cleaned) → native handle with prefix (`@`, `u/`, `discord.gg/`) → detector-captured handle from url → humanised store slug → null. Feed platforms read the handle (not the latest item title); Apple artist from the `input` path. On the wire as `RoutingConnectionResource.accountName`; dashboard `accountLabel()` prefers it. Live rows: `@veritasium`, `Top Choice Restaurant` (uber_eats), `Ruh Wollongong` (bopple), `Atolea Jewelry` (shopify og:title), `@gypsea_lust`, `hbomberguy`, `astralcodexten`. Tests. og:title fetch = the existing `EnrichLinkCardJob` (F4) — payload.name → cleaned. No new column needed.

## W7 dashboard + review fixes (06:20–06:40)

- **Dashboard (e1c4bfa)** — `PlatformsPage` honours `?platform=` (opens the sheet on load, `useSearchParams` under Suspense); `WireConnection.accountName/avatarUrl` on the routing wire; table label = backend accountName → /accounts label → URL. Live: labels for 12+ platforms read the human name (`@veritasium`, `Vision Hair Studio`…).
- **Fresh-eyes review fixes (3b319995c)** — `KeyClass::primaryArtist` keeps a single-letter tail (`Lil Nas X` → `lil nas x`; `Simon & Garfunkel` → `simon`); subreddits label `r/…`; Resync skips ingest sources run <10 min ago and only tolerates a missing ingest schema (scoped `QueryException` catch); `content:retire-legacy-gphoto-records` (idempotent; dry-run 0/0 on dev — for the prod deploy). Test updated (`ProjectionTest` PRIMARY case).

## W6 — Fresha + services categories (06:40–07:25)

- **Team picker in the manage sheet** — `PlatformSheet` renders `FreshaTeamStep` (exported from the connect flow) for `fresha`. Live as partna: 4 real team members (Vision Hair Studio, Melbourne), pick Chetan → `POST /platforms/fresha/selection` 200, selection mode=employee.
- **BUG: services pool empty after picking (fixed, c526bcf5e)** — `saveSelection` composes services from `content.*`, but the Fresha ingest source had NEVER run: `SourceProvisioner` only bumped `next_attempt_at`, and the observer's eager path fired on `created` only. Fix: provisioner returns `'reselected'` when `selection_ref` changes; observer runs eagerly on `created|reselected`; `FreshaConnector` `eagerOnConnect: true` (one free GraphQL call). Live: re-pick → run in ~75s → 35 services (Chloe), 40 storewide, 35 (Ciel). Fresha tests: `Http::fake()` in the fixture (the eager run must not leave the test) + reuse the stream row.
- **BUG: currency null on every Fresha service (fixed)** — the booking-flow response carries `price.formatted` only ("from $360"). Connector resolves the venue's ISO currency once from the location page's `__NEXT_DATA__` (`"currency":"AUD"`), bookmarks it in the stream cursor, stamps records; projector prefers it. Live: 35/35 AUD; dashboard shows `A$50` → `$50` (locale).
- **BUG: departed services never hid (fixed)** — `StreamSpec::mayDelete()` required an order field, so an unordered exhaustive catalogue (Fresha) could never tombstone; a business narrowing storewide(40) → Ciel(35) kept publishing the 5 storewide-only services. Fix: `deletesOnExhaustive` opt-in on the stream (Fresha only; actor menus untouched — see X-items), selection change pre-charges `absent_runs = tombstone_runs-1` so the very next run folds, `LiveSourceScope` honours `source_items.removed_at`. Live: storewide→Ciel → library 40 → 35, `selection.services` 35. Test expectations updated (`FreshaConnectorTest`, `DocumentBuilderRuleOpsTest` negated from_source).
- **R11 business narrowing** — `POST /platforms/fresha/selection/storewide` (capability-gated, `can_book_storewide`); `FreshaTeamStep` shows "Show one team member's services instead" (storewide) / "Book the whole venue instead" (employee) for storewide-capable accounts. Live as business: employee → storewide (run, 40) → employee (run, 35) round trip in the sheet.
- **Services/menus categories UI** — Category column + facet (names off `wire.collections`, vendor order) on `/services` and `/menus`; `Categories` sheet (create / inline rename / delete with confirm / drag reorder; synced vendor categories listed read-only). `lib/queries/categories.ts` gains rename/delete/reorder over the existing endpoints. Live: 12 Fresha categories listed; create → rename → delete round trip against `/service-categories`. Fresha source badge now draws (PoolResolver derives `platform`+`url` from the item's connection when it has no url of its own).

## W8 — item sheets: provenance + overrides (07:25–07:55)

- **Wire (e6d4efe5e)** — per item `overrides: ["f_text.body", …]` and `sources: [{kind, platform, accountName, lastSeenAt, lastSyncedAt, autoSync, active}]`; pool-level `linkRoster` (= `ItemLinkRules::ROSTER`). Both stripped from the public wire (`DASHBOARD_ONLY_ITEM_KEYS`). Ingest cadence read separately (no `ingest.*` join on the public hot path; missing schema → "never"). Fresha rows read the venue name (`selection.storeName`).
- **Dashboard (718821e)** — item sheet: **Links** rows (glyph, host, Synced/Manual, remove for manual) + "Add a link for…" (roster from the wire minus synced/linked) → `PUT/DELETE /content/items/{id}/links/{platform}`; **Sources** rows (account name, platform, Auto-sync, "Synced 15m ago"/Paused/Manual). `syncStateKnown` → true for every column now that `overrides[]` ships, so every field is a real `SyncedField` (padlock / "Not syncing" / Restore). Live on `/watch`: add Vimeo (200) → roster shrinks to Twitch → remove (200) → back; description override → "Not syncing" → Restore (DELETE 200) → Synced.
- **Full suite (full-tests-4.log)**: 8349 passed, 2 failed → both were stale expectations, updated (35a968571). Merged `overnight/w6-w9` → `development`, pushed; monorepo main pushed.

## W10 — Apify actor probes (decision) + W11 X-items (07:55–08:30)

- **W10 (report: `W10-apify-actor-probes.md`, US$0.10 spent)** — Spotify: no artist-driven actor returns the full track catalogue with ISRC (`khadinakbar~*` cap at 10 top tracks; `nifty.codes~spotify-artistdiscography-scraper` is release-level, 22/22 releases, no tracks/ISRC). SoundCloud: current actor healthy (real ISRCs). Instagram: `apify~instagram-reel-scraper` works (3 reels, `videoUrl`); `apify~instagram-scraper` posts mode timed out at 120s. GB: `enckay~google-maps-places-extractor` listed, not probed. **Decision: no failover code tonight** — no probed backup is a drop-in for the working music/GB actors, and swapping a healthy actor for an unverified one is risk without benefit. The failover shape (ordered `actors[]` per platform, budget claimed once per logical run) is written up in the report for when a real backup exists.
- **X2 (fixed, 407a449)** — Platforms table labels come off the routing wire (`accountName` + `+N` from the wire's own count); the per-platform `/accounts` fan-out (~100 requests) is gone. Live: 0 `/accounts` requests on load, 9 rows all named.
- **X4 (fixed, e8e0c2d0f)** — the "unnamed dish" had `f_text.headline = "Beef Brisket Toastie"` but `headline_cache = NULL` (missed its projection refresh; root cause historic, not reproducible). `content:refresh-item-caches` (stale-only default, `--all`, `--dry-run`; daily 03:25) + `ProjectionWriter::refreshCachesFor`. Dev: 1 item healed.
- **W8 parity sweep (`W8-parity-sweep.md`, sonnet)** — its headline (shop/events/links pools "static fixtures") was WRONG (verified: all three render via `PoolPage` → `/content/pools/{pool}`; the fixtures are dev-showcase files nobody imports) — corrected in the report. Real candidates for the owner: routing-suggestions inbox (built, unmounted since 2026-08-16), booking/reservations smart-detect + status, IG/GB synced diff+apply, online-ordering pool CRUD (no page), stale `PublicConfigController::integrations()` docblock. Recorded, not built.
- **Fresh-eyes review (opus) of W6/W8 → fixes (27cfe5c87 / 1d15a03)** — (1) delete-guard: pre-charged "announced" absences no longer count toward the 40% ratio (a 10→5 narrowing would have tripped it forever), and a trip now stamps `absent_since` so `clearGuardIfRecovered` cannot clear it in the same run (pre-existing trip→resolve→repeat bug; 2 tests). (2) `saveStorewide` carries `payload.raw.services` (vendor ORDER) forward instead of blanking it, and returns `pending: true` (the storewide menu lands via the eager run). (3) `sources[].lastSeenAt/lastSyncedAt` ISO-8601 (naive strings read as local time in the browser). (4) roster helpers tolerate a pre-W8 cached wire; "Synced just now" wording; `editableOnItem` collapsed. Clean: no paid connector can ever get a `reselected` eager run (only fresha yields a `selection_ref`); failed/partial runs cannot fold absences; public wire strip covers the only public emitter; Fresha host/bookmark/cursor plumbing; all dashboard primitive APIs. **Owner ruling wanted:** a PINNED item whose only source_item is retired by absence folding now hides (pin row kept; restores on reappearance) — hide-not-delete, but say if a pin should outrank departure.
- **Deploy** — dev cloud deployment `depl-a286e012` (24b6601ac) succeeded 21:24Z; `/api/health` 200; later pushes (27cfe5c87) auto-deploy. Note: `cloud command:run development --cmd=…` hangs in this shell (>240s) — post-deploy probes done via HTTP + git deploy record instead.

## Checkpoint — 08:40 (end of run)

- All waves W0–W11 done; `overnight/w6-w9` fully merged into `development` (head `abdb58977`, pushed; Laravel Cloud auto-deploys); monorepo `main` at `1d15a03` (pushed).
- Backend suite: 8349 passed / 0 failed after fixes (`storage/logs/overnight/full-tests-4.log` + targeted reruns). Dashboard `tsc` clean, `eslint` 0 errors (38 pre-existing warnings). Pint clean on hand-written files (the generated `bootstrap/catalog/compiled.php` and `tests/fixtures/Routing/corpus-generated.php` were pint-red before tonight — CI pint gate note for the morning).
- Local: `artisan serve :8000`, 2× `queue:work`, `schedule:work`, dashboard dev on :3000 — all left running. Test account `broken-oven` currently `business`, Fresha = Vision Hair Studio storewide (40 services), YouTube @veritasium, IG @gypsea_lust, GB Lower East by RÜH.
- **Morning decisions wanted:** (1) pinned-but-departed items hide (W6 review #4); (2) actor-fed menus never delete — opt them into `deletesOnExhaustive` too? (3) parity-sweep candidates (suggestions inbox, booking smart-detect, IG/GB synced diff+apply); (4) prod deploy steps recorded in W3/W4 (retire SQL, `content:retire-legacy-gphoto-records`, clear youtube cursors).

## Morning session (08:45–11:50) — owner rulings R17–R20 + new asks

- **R17** pinned-but-departed items HIDE (no change). **R18 (43164dfa1)** Uber Eats / DoorDash / Square menu streams `deletesOnExhaustive`; the same live run surfaced and fixed a `collection_items_pkey` duplicate-key that had been failing the whole projection chunk on RÜH's Uber Eats run (137 dishes, 0 memberships) → `insertOrIgnore` for memberships. **R20** runbook `docs/2026-08-18-prod-deploy-steps.md`.
- **Audit of the overnight brief vs the plan** — two genuinely missed items found and queued: gumroad/mixcloud/tidal connect (#17, now done), identity "possible duplicate" chip + IG double-run check (#18). Everything else in the brief maps to a done wave or a queued task.
- **Listen restructure (owner, mid-turn) — backend (871cb228a) + dashboard (c275c96)**. Model: every listen item carries `format` = album | ep | single | compilation | track | episode; a track carries `album` (new `f_catalog.collection_title`, migration `20260819001200`) and `trackNumber`. Apple: format from " - Single"/" - EP" suffix (stripped from the title) or track count; songs deduped ONE per title (own collection > album > earliest) — iTunes returned the same song on 3–5 compilation "albums" ("The Less I Know The Better" ×5); albums deduped per (title, format). Bandcamp track pages = singles. **Auto rule per format**: `latest_per_auto_source` now evaluates one arm per toggle group — releases/episodes behind `auto_sync_latest`, tracks behind `auto_sync_latest_track` — so Apple publishes its newest release AND its newest song, each switchable; spotify/soundcloud/youtube-music switches renamed to the track key ("Newest track"; dev had 0 rows to migrate, runbook 2g for prod). Caught live: with the arms split, a track passed the RELEASE arm of a track-only source vacuously ("zero newer releases") → the arm now binds the item's own kind (test in PoolLaneTest). Dashboard: Title/Type columns, Artist · Album subtitle, type facet, Type/Album/Track no. in the sheet; From column stacks one circle per source platform off `sources[]`. Sitepage: nothing to change — apps/pages renders no pool items yet (about/contact only), the wire is ready.
- **Spotify (e0241f734)** — releases stream via `nifty.codes~spotify-artistdiscography-scraper` (own budget key `music-spotify_releases`; W10 probe): 22 Tame Impala releases with 640px covers; track covers via keyless oEmbed (cached in the stream cursor, 300→640 CDN rewrite); artist name via oEmbed on the artist URL; releases deduped per (title, format).
- **Cross-platform merge proof (owner: "test until it works")** — Spotify connected, then Apple Music (Tame Impala). Findings and fixes, in order: (1) selection showed all 10 Spotify tracks → the vacuous release-arm bug above; (2) 26 duplicate title groups → Apple compilation copies (dedupe above); (3) "Dracula"/"Loser" singles on both platforms would not merge → same-source POISONING across kinds: Spotify lists "Dracula" the single (release) AND "Dracula" the song (track), the same title|artist value on two kinds that `mayUnion()` never merges anyway; poisoning is now per (source, kind) (Resolver, test); (4) "The Less I Know The Better" still split → the OLD deleted Apple connection's five compilation copies still voted on identity; sources of removed connections are now excluded from resolution (9adf235fc). Dev leftovers from pre-fix runs were retired by hand (recorded per run: 40, 8+3, 2 keys) — prod never had the songs/releases streams. **State at 11:45 (before the final reprojection lands):** listen library 205, 23 items carry BOTH spotify + apple sources/links, selection = Apple newest release ("Hummer" single, both platforms) + Apple newest song + Spotify newest track ("Loser", both platforms); 2 title groups left: "Neverender" (Apple credits Justice — a real disagreement) and "The Less I Know The Better" (pending the reprojection).
- **Menus merge proof** — Souva King (Wollongong) on Uber Eats + DoorDash: 61 dishes, **9 with both platform links, 0 duplicate titles**. Two ops findings on the way: `MenuFetchJob` timed out at 10 min once and killed its worker (local `queue:work --timeout=300` < the job's 600 — local script raised to 900; cloud workers unaffected), and a stale ShouldBeUnique lock from that kill blocked the next fetch until force-released.
- **#17 gumroad / mixcloud / tidal (134f55853 + dashboard 196724a)** — root causes: shop-class surfaces were excluded from the derived brand connect AND `WebsiteLinkHarvester::classify()` returned null for shop hosts, so Gumroad fell in the gap (404 / 422 "unsupported store"); mixcloud/tidal were `notConnectable` embeds. Now: non-provider shop surfaces get the link card at their storefront root (product paths keep the probe), Gumroad captures `{handle}.gumroad.com`, Mixcloud/Tidal detectors + connectable → all three connect by URL and by bare handle (live 200 ×6). Dashboard `DEDICATED_CONNECT` regenerated from the backend route list (109 brands). Corpus regenerated; registry tests updated.
- **After the final reprojection (11:55):** listen library 203, **25 items carry both Spotify + Apple**, dup title groups = 1 ("Neverender", Apple credits Justice). Selection: Hummer single (both platforms), Loser track (both), plus Apple's newest song. **X5 (cosmetic, open):** an Apple song with no `releaseDate` ("Runway Houses City Clouds (2020 Mix)") sorts as the newest by first_seen and takes the Latest tag; fill from its collection's date or sort undated last.
- **#19 category-first curation (owner, mid-turn) — backend d0801ce2d, dashboard 048ef19.** Rulings applied: (a) a dish or service may sit in SEVERAL categories — `ServiceCollections::assignMany` replaces the owner-lane membership set; the 2026-08-14 `max:1` retired in the four service requests (staff tests inverted); menus already multi. (b) Category control on EVERY menu/service item sheet, scraped dishes included (verified: Uber Eats "Tha Gourmet Burger" → Combos + Bangin Burgers; Fresha "LONG HAIR BLOW WAVE" → BALAYAGE + BY APPRENTICE, both PATCH 200 and on the wire). (c) Order lives inside categories: `menu_item`/`service` are no longer pool-orderable (KindRegistry `CATEGORY_ORDERED_KINDS`) → the tables are select-mode with a bulk "Remove N from site" (verified 61 → 59); the Categories sheet expands each category to its items in `collectionPositions` order (new on the wire) with drag-reorder inside it (menus → `POST /platforms/menu/items/reorder`, services → `POST /services/reorder-layout` full layout), remove-from-category, and "Add here" (opens the add sheet with the category preselected — verified). Category order = the existing drag in the same sheet.
- **#18 identity candidates + IG double run (228833cf9 / dashboard 28ccbe4)** — `duplicateCandidates[]` on the wire (dashboard-only), `POST /content/items/{item}/identity {other, verdict}` writes `identity_decisions` per coord pair, dismisses the candidate and reprojects the involved ingest sources (`ReprojectSourcesJob`); dashboard: "Duplicate?" chip in the From column + a Possible-duplicates section in the item sheet with "Same — merge" / "Different". Live: the one open candidate ("My Old Ways" vs its live version) → `different` → 202, 1 decision, 3 sources reprojected. **IG double Apify run per connect: CONFIRMED and fixed** — the effects ledger showed one ingest actor run per connect on top of `InstagramConnectJob`'s own `fetchProfile`; `InstagramScraper::fetchProfileResult` now keeps a fetched profile for 15 min (`partna.instagram.profile_reuse_seconds`) so the second lane reads it back.
- **#16 final connect sweep (business account, 236 cases over 89 keys, `sweep-final.json`)** — 64 keys connect with a 2xx; the 25 without are 22 booking brands + Square (403 — booking is partna-only by capability, matches W1's "as partna" run), circle/ordermate (fixture artefacts) and bandcamp's dead fixture artists (kingkrule connects 200). Then every never-run live ingest source was run once: pools now — menus lib 95 (0 duplicate titles, 10 dishes carrying both Uber Eats + DoorDash), listen 220 (25 multi-source; the remaining same-title groups are a song beside its own single — different Types, by design), watch 35 (vimeo 20 + youtube 15, selection = newest per source), events 5 (humanitix; the eventbrite organisers currently list no events), media 22 (IG 12 + GB 10, selection 10 = N=5 per source), reviews 5 (GB), services 40 (Fresha), custom_links 18, shop 8. Two identity fixes fell out of the sweep: `OfferingName` floor 8 → 4 (a short dish name is one dish across an owner's platforms), and the deleted-connection exclusion above.
- **#13 parity (dashboard 5b… see main)** — routing suggestions inbox on the Platforms page ("Found on your platforms": Add / Not now, blocked reasons spelled out; live: 3 rows → dismissed Tidal → 2, `POST …/dismiss` 200); the reservations smart-detect suggestion (OpenTable link Google lists for the place) folded into the same inbox with "Use it" → `POST /platforms/opentable/connect`; "Found on Instagram / Google" section in the Instagram and Google Business sheets listing the scrape's findings with Connected / Connecting… / Not used + "Use this one" (`synced/apply`) — live on the GB sheet: 4 findings, all Connected. Booking `detect` gets no separate UI: the connect panel already classifies a pasted URL through the router, and the sweep shows every booking brand connecting directly.
- **Fresh-eyes review #2 (opus) of the morning diffs → fixes (6f7c97150 / dashboard c0b94ab).** P1s: (a) an ingest-lane dish (uber_eats:/doordash: coord) 404'd on the category PATCH (`ManualMenuItems::find` is manual-lane only) → categories-only path in `updateItem` (live 200 on "Tha Gourmet" DoorDash row); (b) `collection_items` PK is (collection, item) without source_id, so the owner lane silently no-op'd over a vendor membership → `assignMany` compares against every lane; (c) menu in-category reorder built its member set from the manual lane → all lanes via `MenuCollections::memberIds`, and it now writes `collection_items.position` too; (d) services layout 422'd without the Uncategorised block → the sheet sends `{id:null}` with every uncategorised service. P2/P3: Spotify releases skip non-artist connections (no paid start for an empty answer), oEmbed misses no longer cached, Apple song dedupe gets a stable trackId tiebreak, the third `resolveItems` read excludes removed connections, Instagram profile reuse moved onto `CacheLockService::rememberLockedNullable` (thin/failed never cached; the RawCacheCallGuard's sanctioned path), `ReprojectSourcesJob` hygiene, `ResolvesOwnedItem` trait shared by the content-item controllers (DAST IDOR variant), guard/golden/service tests updated (golden master 136 → 139 with gumroad/mixcloud/tidal). Flagged, accepted: bulk remove is N sequential calls; `OfferingName` floor 4 is a judgement call (one owner's catalogue).
- **Full suite #6: 8355 passed / 0 failed** (after the last guard registration). Dashboard tsc + eslint clean. Pint clean. Both repos pushed (`development` ef8712f16, monorepo `main` c0b94ab).

## Set live (14:20)

- **What "live" is:** the platform serves from the Laravel Cloud **development** environment (`dev-api.partna.au`, dev Supabase) — the `production` environment has been **stopped since 2026-07-26** (last deploy 265f9aa43, `api.partna.au` → 404, `production` branch 1,313 commits behind), and the Vercel dashboard (`app.partna.au`, git `main`) + the pages Worker point at dev-api. So the runbook's one-off steps were run against the LIVE (dev) database, not the dormant prod Supabase, and nothing was pushed to the `production` branch/env — reviving that is a separate decision (see below).
- Backend: `development` deployed by Laravel Cloud on push (head 182eb307c; `/api/health` 200). One-offs on live: YouTube watch cursors cleared (3), `content:refresh-item-caches` (0 stale), `content:retire-legacy-gphoto-records` (idempotent), pool sections already current (listen 18, media 20), toggle-key migration (0 rows), and every live music ingest source reprojected under the new projectors (10 sources — formats/albums now on the wire for every user).
- Dashboard: Vercel production `partna-qxse7q4ja` built 13:24 from `main` c0b94ab (includes every commit of this session); `app.partna.au` 200.
- Sitepage: `partna-pages` Worker deployed from `main` (`npm run deploy`, version 3772f618 — the previous upload was 2026-07-24); `broken-oven.partna.au` 200. The page body is still the other lane's deliberate blank slate (nothing renders pool items yet), so this session's wire changes are invisible there by design.
- **Open for the owner:** the `production` Laravel Cloud env + prod Supabase (`edplucmvkcnokyygxqsb`) are dormant; if `api.partna.au` should come back, that is: merge `development` → `production`, `supabase db push` against the prod ref (the pending set is everything since July), then the runbook's 2a–2h there. Not done here — nothing serves from it today.

## Session 3 (18:00→) — handoff §3A listen second pass

Plan: `docs/2026-08-18-handoff-platform-pool-testing.md`. Branch `test/listen-pass-2` off `development` (af144f035). Account `broken-oven` (business). Times AEST.

- **Baseline (18:02)** listen wire before the reset: library 270 / selection 6 / 25 multi-source; 2 dup groups (Neverender = real credit disagreement; "Why Won't You Make Up Your Mind (Erol Alkan Rework)" ×2 on Apple = two compilation copies with different `album` — a real dedupe miss, carried to A1). Selection carried two undated auto rows: **"Imagine by John Lennon" (bandcamp release, no creator/date/cover, tagged Latest)** and X5's Apple song — undated rows outrank dated ones on both arms. Bandcamp releases from kingkrule.bandcamp.com had creator=None, publishedAt=None, thumbnail="" and format=album for what are track pages. YouTube Music tracks arrive as raw video titles ("Tame Impala, JENNIE, Boys Noize - Dracula (JENNIE Remix - … Official Audio)", creator `tameimpalaVEVO`) so they never merge with Spotify/Apple.
- **Reset**: `DELETE /platforms/{spotify,apple/music,soundcloud,bandcamp,youtube-music}` → wire library 0 / selection 0 within 5 s (disconnect = hide, proven again).
- **F21 (probe 18:03)** — `POST /platforms/apple/music/connect` (Men I Trust, `music.apple.com/us/artist/men-i-trust/886240553`) → 202 → status `ready` in <15 s, BUT the listen pool stayed empty: `ingest.sources` row `75cdfb66…` created 08:03:23Z with `next_attempt_at = now`, `last_run_at = null` after 3 min. Root cause: `Manifest::runsEagerlyOnConnect()` is opt-in and only the paid connectors + Fresha opt in; every Free connector (apple_music, apple_podcast, bandcamp, youtube, youtube_music, vimeo, eventbrite, humanitix) waits for the 15-min `ingest:dispatch` tick — connect Apple Music, see nothing for up to 15 min. Fix: free connectors run eagerly too (claimed dispatch, idempotent, no spend).
- Also on that connect: `connect/status` `name` = **"Equus Caballus"** (their newest album), not "Men I Trust" — checked in the connections table below.
- **F21 fix** — `Manifest::runsEagerlyOnConnect()` = opt-in OR `cost === Free`; observer docblock; `EagerRunOnConnectTest` inverted for the free case (bandcamp now dispatches one CLAIMED run; auto_sync stays true). Suite fallout: 33 Ingest tests whose helpers hand-insert the stream row after creating a free connection (the eager run under the sync test queue minted it first) → `Bus::fake([RunSourceJob::class])` in their `beforeEach` (the GB precedent, generalised; 5 files). **Live proof:** `POST /platforms/bandcamp/connect` (menitrust) at 08:30:43Z → `ingest.sources` row created 08:30:49Z → run `74b8ceec…` started **08:30:50Z** (1 s later, not the 15-min tick), ok, 10 records, wire library 173 → 181 at 08:31:31Z.
- **Ops** — both local `queue:work` processes had exited silently by ~08:10Z (default `--memory=128` exceeded, no supervisor), which stranded the Spotify eager run's claim for 10 min (`in_flight_since` set, no `ingest.runs` row, queue empty). `restart-workers.sh` now wraps each worker in a `while true` loop with `--memory=512` and logs exits. Claim released by hand, run re-dispatched (30 records ok).
- **X5 fix (dated beats undated)** — `SectionCandidates::connectionSourceLatestArm` "newer" predicate: a dated row is newer than any undated row, then `COALESCE(published, first_seen)`, then id; the `recency` order sorts `(published IS NOT NULL) DESC` first; `PoolResolver::latestItemId` ranks `[dated, at]`. Regression test in `PoolLaneTest` (red without the fix on both the auto arm and the media N-window order). Live: after the Men I Trust reset the selection is Apple newest song "Show Me How (Remix) [Mixed]" (2025-12-15) + Spotify newest track "Come Back Down" (both platforms) + release "Equus Caballus" (both) — no undated row.
- **Apple album title** — a track's `album` read "I Hope to Be Around - Single" (raw `collectionName`) while its release read "I Hope to Be Around". `MusicTrackProjector::albumTitle()` hook; `AppleMusicTrackProjector` strips through `AppleMusicReleaseProjector::formatFromName`. Reprojected the test source: 0 tracks with " - Single"/" - EP" in `album`.
- **A1 result so far (Apple → Spotify → Bandcamp, Men I Trust)**: Apple 173 items (144 tracks / 20 singles / 9 albums, 0 dup groups, every track carries `album`+`trackNumber`); Spotify eager run 30 rows (10 tracks + 20 releases) → **all 30 merged with Apple** (library stayed 173, 30 multi-source, 0 dup groups); selection = 1 release + 2 tracks as designed (Apple's newest song, Spotify's newest track, the shared newest release).
- **F23 (fixed)** — Spotify release cover on the wire was `"https://i.scdn.co/… || https://i.scdn.co/… || …"` (three sizes joined by " || " — the nifty.codes dataset exports `All Cover Art URLs` as ONE string; the adapter's string branch took it verbatim; handoff A5 "verify against a second artist" caught it). `SpotifyReleasesAdapter` splits the string and keeps the 640px variant. Unit tests (`SpotifyReleasesAdapterTest`, new). Re-run inside the effect-freshness window = same dataset, no new spend.
- **F24 (fixed)** — Bandcamp: `data-client-items` holds only the releases the /music page did NOT render as markup (menitrust: 16 `<li>` + 10 blob), so the connector landed the ten OLDEST releases and missed every recent one (Equus Caballus…); neither half carries artist/date so every Bandcamp release was undated + uncredited (kingkrule's "Imagine by John Lennon" in the baseline, 8/10 unmerged Men I Trust releases with `creator=None`). Connector now reads the grid AND the blob (union by page path), credits `data-band` name, and fetches each undated release page (pooled `getMany`, cap 60/run) for JSON-LD `datePublished` / `byArtist` / `numTracks` / full-size art; projector v3 maps `numTracks` to album/ep/single with Apple's 3/6 thresholds. Test with the real page shape (`BandcampConnectorTest` F24 case).
- **Best cover** — every music cover was minted with unknown dims, so `PoolResolver::cover()`'s "largest known area" always fell back to source priority (a Spotify 640 beat Apple's 1200 on merged releases). New `ArtworkDims::infer()` declares dims from the CDN path (Apple `1200x1200bb`, Spotify `b273`=640, Bandcamp `_10`=1200, SoundCloud `t500x500`) at mint (`dims_confidence='declared'`). Unit + writer tests. Existing assets need a one-off backfill (below).
- **Perf note (not a bug)** — locally every query to the Supabase pooler costs ~150–200 ms; the projector writes ~11 queries per record inside a per-record transaction, so Apple's 173 records took 525 s (soundcloud 50 → 7 s is the same code with fewer facets). Same run on Laravel Cloud (co-located) is seconds. Left as is.
- **X6 (pre-existing, not mine)** — `SubdomainChangeTest` "redirects old subdomain" + `PublicSiteControllerShowTest` ×2 fail on `development` head af144f035 before any change of this session (301 expected, 404). Landed with the other lane's routing/public-suffix commits between a776fb6e2 and af144f035; needs their owner.
- **F25 (fixed)** — Apple Podcasts (Song Exploder, `apple/podcast/connect` 200): the eager `RunSourceJob` hit its 300 s timeout locally (200 episodes × ~3 s pooler latency), leaving `ingest.runs` open and the projection half-written — 200 `episode` items with **no headline/url/date/cover** (source items + identity keys land per record, facets in a later pass); the next run saw `records_changed 0` and projected nothing, so the blanks were permanent (one was even auto-selected). `RunExecutor` now closes any open run of the source as `error/RunAbandoned` and projects THIS run regardless of change; `RunSourceJob::failed()` closes the run row itself, and a latest `error` run also forces projection. Test (`RunExecutorProjectionTest` F25). **Live proof ×2:** podcast source — run 2 at 08:52:34Z closed the 08:38 row as RunAbandoned and re-projected → 200 episodes, 0 blank, newest ("Mitski - Your Best American Girl", creator "Song Exploder", `format=episode`) auto-selected on the episode arm; JB Apple source — worker timeout at 09:12:22 (row closed `error/TimeoutExceededException` by `failed()`), CLI re-run at 09:12:42 with `records_changed 0` still projected → 0 blank of 109.
- **F26 (fixed)** — a deferred connect whose FIRST fetch fails terminally (wrong Apple artist id 1041521104 → "Could not find that Apple Music artist") left an `is_active` row with `last_refresh_status=unavailable`: the routing wire listed it as `accountName: "Japanese Breakfast"` (derived from the input path), `/accounts` listed it (`name: null`), and an ingest source was provisioned. `ConnectFetchJob::markTerminal` soft-deletes a row with `last_refreshed_at IS NULL`; new `connectStatusRow()` (trait) lets Generic/bespoke/Fresha status polls still read the trashed failed row so the modal shows the error; `writeAccountConnection` keeps `last_refreshed_at` on a pending RE-write so a failed re-connect keeps the previously-good row. Tests (`DeferredConnectTest` ×2, `WriteAccountConnectionPendingTest` updated). Phantom row on the test account removed by hand (`DELETE accounts/acct-09918c…`).
- **F27 (fixed)** — Spotify sources releases since the restructure but declared only the `auto_sync_latest_track` switch, so its release arm was un-switchable: with Apple's + Bandcamp's release switches off, "Equus Caballus" stayed selected through Spotify. Registry now declares `auto_sync_latest` ("Newest release") + the track switch for Spotify. Live: all three release switches off → no release in the selection; back on → Equus Caballus returns. Test (`DisplaySettingsTest`).
- **A3 switches (live, Men I Trust + podcast)**: Apple release OFF → song stays ✓; Apple song OFF → "Show Me How (Remix)" leaves, "Come Back Down" stays via Spotify's track arm ✓; Spotify track OFF → it leaves ✓; podcast OFF → episode leaves ✓; all ON → everything back ✓.
- **A1 final (Men I Trust)**: Bandcamp re-run 26 releases (was 10), dated + credited: "Equus Caballus" now `apple-music + bandcamp + spotify` (3 links), "I Hope to Be Around" ×3 platforms; every Bandcamp release credited "Men I Trust". SoundCloud `menitrust` is an empty squatter account (`track_count: 0` on the profile; actor "no tracks" is correct) — SoundCloud leg run on **Japanese Breakfast** instead: SoundCloud 50 tracks + Apple 109 → **49 of 50 SoundCloud tracks merged with Apple** (the one left is a Korean version not on Apple US), every merged track carries `album` from Apple ("Sable (Original Video Game Soundtrack)", "Jubilee"…). Podcast: 10 same-title episode pairs on `apple-podcast` (Song Exploder re-runs old episodes under the same title, different dates) stay separate by the same-source rule — correct, they are two episodes.
- **A6 dashboard**: the Browser pane refused every navigation to localhost:3000 / 127.0.0.1:3000 this session (origin approval needs the owner present), so no screenshots. Code check only: `components/blocks/pools/listen-page.tsx` carries the Type chip (`FormatChip`), Artist · Album subtitle, `FORMAT_FACET`, `SourceStack`, and `pool-item-provenance.tsx` the Duplicate? flow — all shipped/verified in the morning session; the wire they read is what this session re-proved. No dashboard change was needed for 3A.
- **Backend merged → `development` 5f6139e16 (pushed 19:20 AEST)**; full suite before merge: 8418 passed / 4 failed → 1 was the pinned `last_refreshed_at` expectation (updated in ef90e2477), 3 are X6 (pre-existing).
